[frontend] `ShrinkPass` -- support `(when)` & `(unless)`

[frontend] test `if` by test of eoc

fix import of Meta

```js
import { type TokenMeta as Meta } from "@xieyuheng/x-sexp.js"
```

ambs 'import { type TokenMeta } from "@xieyuheng/x-sexp.js"'

rename match functions to parse functions
rename syntax/ to parse/


# backend -- module

[backend] `Stmt` -- `Import`
[backend] `Stmt` -- `Include`

# backend -- bundling

[backend] `Mod` -- bundle

# compiler

[compiler] `compileToX86Assembly`
