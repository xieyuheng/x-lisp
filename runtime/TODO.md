[runtime] `value` -- little.c -- setup
[runtime] `value` -- `little_p`
[runtime] `value` -- `little_hashtag_p` encoding little hashtag inline
[runtime] `value` -- `little_symbol_p` encoding little symbol inline

```scheme
#t
#f
#null
#void
```

[runtime] builtin
[runtime] GC -- mark and sweep
