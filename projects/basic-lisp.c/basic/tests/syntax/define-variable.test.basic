(define-variable x
  (block entry
    (return 1)))

(define-function (main)
  (block entry
    (perform (assert-equal x 1))
    (perform (println x))
    ;; 2 @ref x @assign
    ;; (perform (assert-equal x 2))
    (return)))
