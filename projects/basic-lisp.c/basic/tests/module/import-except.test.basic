(import-except "module-three" three)

(define-function (three) (block entry (return 3)))

(define-function (main)
  (block entry
    (= x (one)) (perform (assert-equal x 1))
    (= x (two)) (perform (assert-equal x 2))
    (= x (three)) (perform (assert-equal x 3))
    (return)))
