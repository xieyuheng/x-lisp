(type-error-module identity-type-error-2)

(claim identity
  (polymorphic (A)
    (-> A A)))

(define (identity x) x)

(define-test identity-test
  (the (polymorphic (A) (-> A A)) identity)
  (the (polymorphic (A B) (-> A B)) identity))
