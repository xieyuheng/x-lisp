(module test-suite)

(define (S x y z) (x z (y z)))
(define (K x y) x)
(define (I x) x)

(define SKI (S K I))

(define SKI-tuple
  (= S (lambda (x y z) (x z (y z))))
  (= K (lambda (x y) x))
  (= I (lambda (x) x))
  (@tuple S K I))
