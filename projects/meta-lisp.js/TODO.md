rename .lisp to .meta
rename .lisp in design/

# record syntax

`Extend` & `ExtendMut` -- as `Exp`
parse `(extend)` & `(extend!)`
test (extend) edge case:

```caml
\r → if True then {x = 2 | r} else {y = 2 | r}
```

# basic-lisp

bring basic-lisp back

# stack-lisp

design stack-lisp -- stack-lisp.c
