(claim identity (polymorphic (A) (-> A A)))

(define (identity x) x)

(claim main (-> void-t))

(define (main)
  (= add1₁ (iadd 1))
  (assert-equal 2 (add1₁ 1))
  (= add1₂ (identity (iadd (identity 1))))
  identity
  identity
  identity
  (assert-equal 2 (add1₂ 1)))