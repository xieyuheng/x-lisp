;;; Input

(define (S x y z) (x z (y z)))

(define (K x y) x)

(define (main) (S K K) void)

;;; Loaded

(claim S
  (polymorphic (A₉₄ A₉₃ R₇₆)
    (-> (-> A₉₄ (-> A₉₃ R₇₆)) (-> (-> A₉₄ A₉₃) (-> A₉₄ R₇₆)))))

(define (S x y z) (x z (y z)))

(claim K (polymorphic (R₈₁ A₉₆) (-> R₈₁ (-> A₉₆ R₈₁))))

(define (K x y) x)

(claim main (-> void-t))

(define (main) (S K K) void)

