;;; input

(define (S x y z) (x z (y z)))

(define (K x y) x)

(define (main) (S K K) void)

;;; loaded

(define (S x y z) (x z (y z)))

(define (K x y) x)

(define (main) (S K K) void)

;;; desugared

(define (S x y z) (x z (y z)))

(define (K x y) x)

(define (main) (S K K) void)

;;; checked

(claim S
  (polymorphic (A B C)
    (-> (-> A (-> B C)) (-> (-> A B) (-> A C)))))

(define (S x y z) (x z (y z)))

(claim K (polymorphic (A B) (-> A (-> B A))))

(define (K x y) x)

(claim main (-> void-t))

(define (main) (S K K) void)

