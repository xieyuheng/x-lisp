(claim main (-> void-t))

(define (main)
  (= _₁
     (if
         (if (begin (= _₂ (random-dice)) (equal? _₂ 1))
           (begin (= _₃ (random-dice)) (equal? _₃ 2))
           false)
       0
       42))
  (println _₁))