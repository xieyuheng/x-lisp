;;; input

(claim main (-> void-t))

(define (main) (println (iadd (random-dice) (random-dice))))

;;; loaded

(claim main (-> void-t))

(define (main) (println (iadd (random-dice) (random-dice))))

;;; desugared

(claim main (-> void-t))

(define (main) (println (iadd (random-dice) (random-dice))))

;;; checked

(claim main (-> void-t))

(define (main) (println (iadd (random-dice) (random-dice))))

