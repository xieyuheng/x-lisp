;;; input

(claim main (-> void-t))

(define (main)
  (= z (begin (= y (begin (= x (ineg 42)) x)) y))
  (println (ineg z)))

;;; loaded

(claim main (-> void-t))

(define (main)
  (= z (begin (= y (begin (= x (ineg 42)) x)) y))
  (println (ineg z)))

;;; desugared

(claim main (-> void-t))

(define (main)
  (= z (begin (= y (begin (= x (ineg 42)) x)) y))
  (println (ineg z)))

;;; checked

(claim main (-> void-t))

(define (main)
  (= z (begin (= y (begin (= x (ineg 42)) x)) y))
  (println (ineg z)))

