;;; input

(claim main (-> void-t))

(define (main)
  (= x 1)
  (println
   (iadd
    x
    (begin (= x (begin (= y 5) (iadd y x))) (iadd x 100)))))

;;; loaded

(claim main (-> void-t))

(define (main)
  (= x 1)
  (println
   (iadd
    x
    (begin (= x (begin (= y 5) (iadd y x))) (iadd x 100)))))

;;; desugared

(claim main (-> void-t))

(define (main)
  (= x 1)
  (println
   (iadd
    x
    (begin (= x (begin (= y 5) (iadd y x))) (iadd x 100)))))

;;; checked

(claim main (-> void-t))

(define (main)
  (= x 1)
  (println
   (iadd
    x
    (begin (= x (begin (= y 5) (iadd y x))) (iadd x 100)))))

