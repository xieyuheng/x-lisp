;;; Input

(claim main (-> void-t))

(define (main)
  (= x 32)
  (println (iadd (begin (= x 10) x) x)))

;;; Loaded

(claim main (-> void-t))

(define (main)
  (= x 32)
  (println (iadd (begin (= x 10) x) x)))

;;; Checked

(claim main (-> void-t))

(define (main)
  (= x 32)
  (println (iadd (begin (= x 10) x) x)))

