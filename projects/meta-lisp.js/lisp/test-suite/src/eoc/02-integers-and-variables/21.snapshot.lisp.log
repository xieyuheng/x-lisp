;;; Input

(claim main (-> void-t))

(define (main)
  (= y 6)
  (begin
    (= x (begin (= y (ineg 42)) y))
    (println (iadd x y))))

;;; Loaded

(claim main (-> void-t))

(define (main)
  (= y 6)
  (begin
    (= x (begin (= y (ineg 42)) y))
    (println (iadd x y))))

;;; Desugared

(claim main (-> void-t))

(define (main)
  (= y 6)
  (= x (begin (= y (ineg 42)) y))
  (println (iadd x y)))

;;; Checked

(claim main (-> void-t))

(define (main)
  (= y 6)
  (= x (begin (= y (ineg 42)) y))
  (println (iadd x y)))

