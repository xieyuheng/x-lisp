;;; input

(claim main (-> void-t))

(define (main) (println (iadd 20 (iadd 11 11))))

;;; loaded

(claim main (-> void-t))

(define (main) (println (iadd 20 (iadd 11 11))))

;;; desugared

(claim main (-> void-t))

(define (main) (println (iadd 20 (iadd 11 11))))

;;; checked

(claim main (-> void-t))

(define (main) (println (iadd 20 (iadd 11 11))))

