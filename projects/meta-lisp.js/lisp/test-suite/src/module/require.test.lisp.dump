;;; input

(define (main) (assert-equal 1 (@require "module-one" one)))

;;; loaded

(define (main) (assert-equal 1 (@require "module-one" one)))

;;; desugared

(define (main) (assert-equal 1 (@require "module-one" one)))

;;; checked

(claim main (-> void-t))

(define (main) (assert-equal 1 (@require "module-one" one)))

