;;; Input

(claim main (-> void-t))

(define (main) (assert (hash-empty? (@hash))))

;;; Loaded

(claim main (-> void-t))

(define (main) (assert (hash-empty? (@hash))))

;;; Desugared

(claim main (-> void-t))

(define (main)
  (assert (hash-empty? (begin (= hash (make-hash)) hash))))

;;; Checked

(claim main (-> void-t))

(define (main)
  (assert (hash-empty? (begin (= hash (make-hash)) hash))))

