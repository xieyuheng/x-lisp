;;; Input

(claim main (-> void-t))

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; Loaded

(claim main (-> void-t))

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; Checked

(claim main (-> void-t))

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

