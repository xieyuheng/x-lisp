(claim main (-> void-t))

(define (main)
  (= list₁ (make-list))
  (list-push! 1 list₁)
  (list-push! 2 list₁)
  (list-push! 3 list₁)
  (println list₁)
  (println
   (begin
     (= list₂ (make-list))
     (list-push! 1 list₂)
     (list-push! 2 list₂)
     (list-push! 3 list₂)
     list₂))
  (println
   (begin
     (= list₂ (make-list))
     (list-push! 1 list₂)
     (list-push! 2 list₂)
     (list-push! 3 list₂)
     list₂))
  (println
   (begin
     (= list₂ (make-list))
     (list-push! 1 list₂)
     (list-push! 2 list₂)
     (list-push! 3 list₂)
     list₂))
  (println
   (begin
     (= list₂ (make-list))
     (list-push! 1 list₂)
     (list-push! 2 list₂)
     (list-push! 3 list₂)
     list₂))
  (println
   (begin
     (= list₂ (make-list))
     (list-push! 'a list₂)
     (list-push! 'b list₂)
     (list-push! 'c list₂)
     list₂))
  (println
   (begin
     (= list₂ (make-list))
     (list-push! 'a list₂)
     (list-push! 'b list₂)
     (list-push! 'c list₂)
     list₂)))