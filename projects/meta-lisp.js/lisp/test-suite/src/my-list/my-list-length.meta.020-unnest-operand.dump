(claim my-list-length
  (polymorphic (E) (-> (my-list-t E) int-t)))

(define (my-list-length list)
  (if (nil? list)
    0
    (if (li? list)
      (begin
        (= tail₄₁ (li-tail list))
        (= head₄₁ (li-head list))
        (= tail₁ tail₄₁)
        (= head₁ head₄₁)
        (= _₁ (my-list-length tail₁))
        (iadd 1 _₁))
      (error "cond mismatch"))))