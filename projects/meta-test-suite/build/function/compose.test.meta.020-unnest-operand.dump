(claim compose
  (polymorphic (A B C) (-> (-> B C) (-> A B) (-> A C))))

(define (compose f g x) (= _₁ (g x)) (f _₁))

(claim main (-> void-t))

(define (main)
  (= add1₁ (iadd 1))
  (= _₁ (compose add1₁ add1₁))
  (= _₂ (_₁ 1))
  (assert-equal 3 _₂))