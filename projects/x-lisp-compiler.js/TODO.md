[x-lisp] builtin -- for name and arity only
[x-lisp] setup test -- for pass log only

[x-lisp] remove `Apply` (binary) and `ApplyNullary` -- keep `ApplySugar`
[x-lisp] rename `ApplySugar` to `Apply`
[x-lisp] [maybe] `Let1` as optional `name`
