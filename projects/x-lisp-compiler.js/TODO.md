# the

[lisp] `generateDataPredicate` -- use `the`

# design by contract

[lisp] support `Claim`

# pattern match

# later

[pass] `desugarCond` -- report `error` about mismatch -- instead of `assert`
