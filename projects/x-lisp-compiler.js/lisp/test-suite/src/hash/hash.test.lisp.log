;;; Input

(define (main) (assert (hash-empty? (@hash))))

;;; ShrinkPass

(define (main)
  (assert (hash-empty? (let ((hash (make-hash))) hash))))

;;; UniquifyPass

(define (main)
  (assert (hash-empty? (let ((hash₁ (make-hash))) hash₁))))

;;; RevealGlobalPass

(define (main)
  (assert (hash-empty? (let ((hash₁ (make-hash))) hash₁))))

;;; LiftLambdaPass

(define (main)
  (assert (hash-empty? (let ((hash₁ (make-hash))) hash₁))))

;;; UnnestOperandPass

(define (main)
  (let ((hash₁ (make-hash)))
    (let ((_₁ (hash-empty? hash₁))) (assert _₁))))

