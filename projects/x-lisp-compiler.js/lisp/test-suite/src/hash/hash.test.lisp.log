;;; Input

(define (main) (assert (hash-empty? (@hash))))

;;; Loaded

(define (main) (assert (hash-empty? (@hash))))

;;; ShrinkPass

(define (main)
  (assert (hash-empty? (begin (= hash (make-hash)) hash))))

;;; UniquifyPass

(define (main)
  (assert (hash-empty? (begin (= hash₁ (make-hash)) hash₁))))

;;; RevealGlobalPass

(define (main)
  (assert (hash-empty? (begin (= hash₁ (make-hash)) hash₁))))

;;; LiftLambdaPass

(define (main)
  (assert (hash-empty? (begin (= hash₁ (make-hash)) hash₁))))

;;; UnnestOperandPass

(define (main)
  (= hash₁ (make-hash))
  (= _₁ (hash-empty? hash₁))
  (assert _₁))

