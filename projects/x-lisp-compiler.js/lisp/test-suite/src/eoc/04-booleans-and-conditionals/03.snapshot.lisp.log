;;; Input

(define (main)
  (println
   (if
       (and (equal? (random-dice) 1)
            (equal? (random-dice) 2))
     0
     42)))

;;; ShrinkPass

(define (main)
  (println
   (if
       (if (equal? (random-dice) 1)
         (equal? (random-dice) 2)
         #f)
     0
     42)))

;;; UniquifyPass

(define (main)
  (println
   (if
       (if (equal? (random-dice) 1)
         (equal? (random-dice) 2)
         #f)
     0
     42)))

;;; RevealGlobalPass

(define (main)
  (println
   (if
       (if (equal? (random-dice) 1)
         (equal? (random-dice) 2)
         #f)
     0
     42)))

;;; LiftLambdaPass

(define (main)
  (println
   (if
       (if (equal? (random-dice) 1)
         (equal? (random-dice) 2)
         #f)
     0
     42)))

;;; UnnestOperandPass

(define (main)
  (let
      ((_₁ (if
               (if (let ((_₂ (random-dice))) (equal? _₂ 1))
                 (let ((_₃ (random-dice))) (equal? _₃ 2))
                 #f)
             0
             42)))
    (println _₁)))

