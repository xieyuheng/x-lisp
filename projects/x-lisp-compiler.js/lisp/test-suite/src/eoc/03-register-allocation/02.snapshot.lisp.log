;;; Input

(define (main)
  (= x (random-dice))
  (= y (random-dice))
  (println (iadd (iadd x y) 42)))

;;; Loaded

(define (main)
  (= x (random-dice))
  (= y (random-dice))
  (println (iadd (iadd x y) 42)))

;;; ShrinkPass

(define (main)
  (= x (random-dice))
  (= y (random-dice))
  (println (iadd (iadd x y) 42)))

;;; UniquifyPass

(define (main)
  (= x₁ (random-dice))
  (= y₁ (random-dice))
  (println (iadd (iadd x₁ y₁) 42)))

;;; LiftLambdaPass

(define (main)
  (= x₁ (random-dice))
  (= y₁ (random-dice))
  (println (iadd (iadd x₁ y₁) 42)))

;;; UnnestOperandPass

(define (main)
  (= x₁ (random-dice))
  (= y₁ (random-dice))
  (= _₁ (iadd x₁ y₁))
  (= _₂ (iadd _₁ 42))
  (println _₂))

