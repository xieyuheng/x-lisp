;;; Input

(define (main) (println (iadd 20 (iadd 11 11))))

;;; ShrinkPass

(define (main) (println (iadd 20 (iadd 11 11))))

;;; UniquifyPass

(define (main) (println (iadd 20 (iadd 11 11))))

;;; RevealGlobalPass

(define (main)
  ((@primitive-function-ref println 1)
   ((@primitive-function-ref iadd 2) 20 ((@primitive-function-ref iadd 2) 11 11))))

;;; LiftLambdaPass

(define (main)
  ((@primitive-function-ref println 1)
   ((@primitive-function-ref iadd 2) 20 ((@primitive-function-ref iadd 2) 11 11))))

;;; UnnestOperandPass

(define (main)
  (@let1 _₁ ((@primitive-function-ref iadd 2) 11 11)
    (@let1 _₂ ((@primitive-function-ref iadd 2) 20 _₁)
      ((@primitive-function-ref println 1) _₂))))

