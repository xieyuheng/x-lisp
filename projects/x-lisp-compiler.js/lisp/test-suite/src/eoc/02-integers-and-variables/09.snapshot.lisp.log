;;; Input

(define (main) (println (iadd 20 (iadd 11 11))))

;;; ShrinkPass

(define (main) (println (iadd 20 (iadd 11 11))))

;;; UniquifyPass

(define (main) (println (iadd 20 (iadd 11 11))))

;;; RevealGlobalPass

(define (main) (println (iadd 20 (iadd 11 11))))

;;; LiftLambdaPass

(define (main) (println (iadd 20 (iadd 11 11))))

;;; UnnestOperandPass

(define (main)
  (= _₁ (iadd 11 11))
  (= _₂ (iadd 20 _₁))
  (println _₂))

