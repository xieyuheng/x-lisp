;;; Input

(define (main)
  (= x 32)
  (println (iadd (begin (= x 10) x) x)))

;;; Loaded

(define (main)
  (= x 32)
  (println (iadd (begin (= x 10) x) x)))

;;; ShrinkPass

(define (main)
  (= x 32)
  (println (iadd (begin (= x 10) x) x)))

;;; UniquifyPass

(define (main)
  (= x₁ 32)
  (println (iadd (begin (= x₂ 10) x₂) x₁)))

;;; LiftLambdaPass

(define (main)
  (= x₁ 32)
  (println (iadd (begin (= x₂ 10) x₂) x₁)))

;;; UnnestOperandPass

(define (main)
  (= x₁ 32)
  (= x₂ 10)
  (= _₁ (iadd x₂ x₁))
  (println _₁))

