;;; Input

(define (main)
  (= x (iadd (iadd 1 2) (iadd 3 4)))
  (println (iadd x 5)))

;;; Loaded

(define (main)
  (= x (iadd (iadd 1 2) (iadd 3 4)))
  (println (iadd x 5)))

;;; ShrinkPass

(define (main)
  (= x (iadd (iadd 1 2) (iadd 3 4)))
  (println (iadd x 5)))

;;; UniquifyPass

(define (main)
  (= x₁ (iadd (iadd 1 2) (iadd 3 4)))
  (println (iadd x₁ 5)))

;;; RevealGlobalPass

(define (main)
  (= x₁ (iadd (iadd 1 2) (iadd 3 4)))
  (println (iadd x₁ 5)))

;;; LiftLambdaPass

(define (main)
  (= x₁ (iadd (iadd 1 2) (iadd 3 4)))
  (println (iadd x₁ 5)))

;;; UnnestOperandPass

(define (main)
  (= x₁
     (begin
       (= _₁ (iadd 1 2))
       (= _₂ (iadd 3 4))
       (iadd _₁ _₂)))
  (= _₃ (iadd x₁ 5))
  (println _₃))

