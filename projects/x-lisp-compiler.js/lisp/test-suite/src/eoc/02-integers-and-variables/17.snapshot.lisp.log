;;; Input

(define (main) (println (iadd 1 (iadd (random-dice) 1))))

;;; ShrinkPass

(define (main) (println (iadd 1 (iadd (random-dice) 1))))

;;; UniquifyPass

(define (main) (println (iadd 1 (iadd (random-dice) 1))))

;;; RevealGlobalPass

(define (main)
  ((@primitive println 1)
   ((@primitive iadd 2) 1 ((@primitive iadd 2) ((@primitive random-dice 0)) 1))))

;;; LiftLambdaPass

(define (main)
  ((@primitive println 1)
   ((@primitive iadd 2) 1 ((@primitive iadd 2) ((@primitive random-dice 0)) 1))))

;;; UnnestOperandPass

(define (main)
  (@let1 _₁ ((@primitive random-dice 0))
    (@let1 _₂ ((@primitive iadd 2) _₁ 1)
      (@let1 _₃ ((@primitive iadd 2) 1 _₂) ((@primitive println 1) _₃)))))

