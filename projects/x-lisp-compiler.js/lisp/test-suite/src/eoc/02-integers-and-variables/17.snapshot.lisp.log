;;; Input

(define (main) (println (iadd 1 (iadd (random-dice) 1))))

;;; ShrinkPass

(define (main) (println (iadd 1 (iadd (random-dice) 1))))

;;; UniquifyPass

(define (main) (println (iadd 1 (iadd (random-dice) 1))))

;;; RevealGlobalPass

(define (main) (println (iadd 1 (iadd (random-dice) 1))))

;;; LiftLambdaPass

(define (main) (println (iadd 1 (iadd (random-dice) 1))))

;;; UnnestOperandPass

(define (main)
  (let ((_₁ (random-dice)))
    (let ((_₂ (iadd _₁ 1)))
      (let ((_₃ (iadd 1 _₂))) (println _₃)))))

