;;; Input

(define (main) (println (iadd 20 22)))

;;; ShrinkPass

(define (main) (println (iadd 20 22)))

;;; UniquifyPass

(define (main) (println (iadd 20 22)))

;;; RevealGlobalPass

(define (main)
  ((@primitive println 1) ((@primitive iadd 2) 20 22)))

;;; LiftLambdaPass

(define (main)
  ((@primitive println 1) ((@primitive iadd 2) 20 22)))

;;; UnnestOperandPass

(define (main)
  (let ((_₁ ((@primitive iadd 2) 20 22)))
    ((@primitive println 1) _₁)))

