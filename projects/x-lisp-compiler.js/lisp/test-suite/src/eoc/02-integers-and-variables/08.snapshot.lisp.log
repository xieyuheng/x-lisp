;;; Input

(claim main (-> void-t))

(define (main) (println (iadd 20 22)))

;;; Loaded

(define (main) (println (iadd 20 22)))

;;; ShrinkPass

(define (main) (println (iadd 20 22)))

;;; UniquifyPass

(define (main) (println (iadd 20 22)))

;;; LiftLambdaPass

(define (main) (println (iadd 20 22)))

;;; UnnestOperandPass

(define (main) (= _₁ (iadd 20 22)) (println _₁))

