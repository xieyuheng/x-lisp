;;; Input

(define (main) (println (iadd 20 22)))

;;; ShrinkPass

(define (main) (println ((iadd 20) 22)))

;;; UniquifyPass

(define (main) (println ((iadd 20) 22)))

;;; RevealGlobalPass

(define (main)
  ((@builtin-function-ref println 1)
   (((@builtin-function-ref iadd 2) 20) 22)))

;;; LiftLambdaPass

(define (main)
  ((@builtin-function-ref println 1)
   (((@builtin-function-ref iadd 2) 20) 22)))

