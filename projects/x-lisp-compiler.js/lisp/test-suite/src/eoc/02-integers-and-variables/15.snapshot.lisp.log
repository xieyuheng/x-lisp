;;; Input

(claim main (-> void-t))

(define (main)
  (= x (begin (= x 4) (iadd x 1)))
  (println (iadd x 2)))

;;; Loaded

(define (main)
  (= x (begin (= x 4) (iadd x 1)))
  (println (iadd x 2)))

;;; ShrinkPass

(define (main)
  (= x (begin (= x 4) (iadd x 1)))
  (println (iadd x 2)))

;;; UniquifyPass

(define (main)
  (= x₁ (begin (= x₂ 4) (iadd x₂ 1)))
  (println (iadd x₁ 2)))

;;; LiftLambdaPass

(define (main)
  (= x₁ (begin (= x₂ 4) (iadd x₂ 1)))
  (println (iadd x₁ 2)))

;;; UnnestOperandPass

(define (main)
  (= x₁ (begin (= x₂ 4) (iadd x₂ 1)))
  (= _₁ (iadd x₁ 2))
  (println _₁))

