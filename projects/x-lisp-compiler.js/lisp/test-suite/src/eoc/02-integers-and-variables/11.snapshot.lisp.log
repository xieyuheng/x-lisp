;;; Input

(define (main) (println (iadd (iadd 1 2) (iadd 3 4))))

;;; Loaded

(define (main) (println (iadd (iadd 1 2) (iadd 3 4))))

;;; ShrinkPass

(define (main) (println (iadd (iadd 1 2) (iadd 3 4))))

;;; UniquifyPass

(define (main) (println (iadd (iadd 1 2) (iadd 3 4))))

;;; LiftLambdaPass

(define (main) (println (iadd (iadd 1 2) (iadd 3 4))))

;;; UnnestOperandPass

(define (main)
  (= _₁ (iadd 1 2))
  (= _₂ (iadd 3 4))
  (= _₃ (iadd _₁ _₂))
  (println _₃))

