;;; Input

(define (main) (println (iadd (iadd 1 2) (iadd 3 4))))

;;; ShrinkPass

(define (main) (println (iadd (iadd 1 2) (iadd 3 4))))

;;; UniquifyPass

(define (main) (println (iadd (iadd 1 2) (iadd 3 4))))

;;; RevealGlobalPass

(define (main) (println (iadd (iadd 1 2) (iadd 3 4))))

;;; LiftLambdaPass

(define (main) (println (iadd (iadd 1 2) (iadd 3 4))))

;;; UnnestOperandPass

(define (main)
  (let ((_₁ (iadd 1 2)))
    (let ((_₂ (iadd 3 4)))
      (let ((_₃ (iadd _₁ _₂))) (println _₃)))))

