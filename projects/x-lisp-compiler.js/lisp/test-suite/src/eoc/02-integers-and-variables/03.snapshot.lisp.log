;;; Input

(define (main)
  (= y (begin (= x 20) (begin (= z 22) (iadd x z))))
  (println y))

;;; Loaded

(define (main)
  (= y (begin (= x 20) (begin (= z 22) (iadd x z))))
  (println y))

;;; ShrinkPass

(define (main)
  (= y (begin (= x 20) (= z 22) (iadd x z)))
  (println y))

;;; UniquifyPass

(define (main)
  (= y₁ (begin (= x₁ 20) (= z₁ 22) (iadd x₁ z₁)))
  (println y₁))

;;; RevealGlobalPass

(define (main)
  (= y₁ (begin (= x₁ 20) (= z₁ 22) (iadd x₁ z₁)))
  (println y₁))

;;; LiftLambdaPass

(define (main)
  (= y₁ (begin (= x₁ 20) (= z₁ 22) (iadd x₁ z₁)))
  (println y₁))

;;; UnnestOperandPass

(define (main)
  (= y₁ (begin (= x₁ 20) (= z₁ 22) (iadd x₁ z₁)))
  (println y₁))

