;;; Input

(claim main (-> void-t))

(define (main)
  (= z (begin (= y (begin (= x (ineg 42)) x)) y))
  (println (ineg z)))

;;; Loaded

(define (main)
  (= z (begin (= y (begin (= x (ineg 42)) x)) y))
  (println (ineg z)))

;;; ShrinkPass

(define (main)
  (= z (begin (= y (begin (= x (ineg 42)) x)) y))
  (println (ineg z)))

;;; UniquifyPass

(define (main)
  (= z₁ (begin (= y₁ (begin (= x₁ (ineg 42)) x₁)) y₁))
  (println (ineg z₁)))

;;; LiftLambdaPass

(define (main)
  (= z₁ (begin (= y₁ (begin (= x₁ (ineg 42)) x₁)) y₁))
  (println (ineg z₁)))

;;; UnnestOperandPass

(define (main)
  (= z₁ (begin (= y₁ (begin (= x₁ (ineg 42)) x₁)) y₁))
  (= _₁ (ineg z₁))
  (println _₁))

