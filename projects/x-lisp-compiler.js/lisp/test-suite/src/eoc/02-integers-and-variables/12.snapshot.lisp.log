;;; Input

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; Loaded

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; ShrinkPass

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; UniquifyPass

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; RevealGlobalPass

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; LiftLambdaPass

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; UnnestOperandPass

(define (main)
  (= _₁ (iadd 1 2))
  (= _₂ (iadd 4 5))
  (= _₃ (iadd 3 _₂))
  (= _₄ (iadd _₁ _₃))
  (println _₄))

