;;; Input

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; ShrinkPass

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; UniquifyPass

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; RevealGlobalPass

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; LiftLambdaPass

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; UnnestOperandPass

(define (main)
  (let ((_₁ (iadd 1 2)))
    (let ((_₂ (iadd 4 5)))
      (let ((_₃ (iadd 3 _₂)))
        (let ((_₄ (iadd _₁ _₃))) (println _₄))))))

