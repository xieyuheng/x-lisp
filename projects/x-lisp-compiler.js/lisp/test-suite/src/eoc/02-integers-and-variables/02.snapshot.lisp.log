;;; Input

(claim main (-> void-t))

(define (main)
  (= y (begin (= x 20) (iadd x (begin (= x 22) x))))
  (println y))

;;; Loaded

(define (main)
  (= y (begin (= x 20) (iadd x (begin (= x 22) x))))
  (println y))

;;; ShrinkPass

(define (main)
  (= y (begin (= x 20) (iadd x (begin (= x 22) x))))
  (println y))

;;; UniquifyPass

(define (main)
  (= y₁ (begin (= x₁ 20) (iadd x₁ (begin (= x₂ 22) x₂))))
  (println y₁))

;;; LiftLambdaPass

(define (main)
  (= y₁ (begin (= x₁ 20) (iadd x₁ (begin (= x₂ 22) x₂))))
  (println y₁))

;;; UnnestOperandPass

(define (main)
  (= y₁ (begin (= x₁ 20) (= x₂ 22) (iadd x₁ x₂)))
  (println y₁))

