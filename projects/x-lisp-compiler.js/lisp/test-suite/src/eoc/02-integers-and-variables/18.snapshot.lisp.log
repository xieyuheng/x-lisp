;;; Input

(define (main)
  (println (iadd (iadd (iadd (iadd (random-dice) 1) 1) 1) 1)))

;;; ShrinkPass

(define (main)
  (println
   ((iadd ((iadd ((iadd ((iadd (random-dice)) 1)) 1)) 1)) 1)))

;;; UniquifyPass

(define (main)
  (println
   ((iadd ((iadd ((iadd ((iadd (random-dice)) 1)) 1)) 1)) 1)))

;;; RevealGlobalPass

(define (main)
  ((@builtin-function-ref println 1)
   (((@builtin-function-ref iadd 2)
     (((@builtin-function-ref iadd 2)
       (((@builtin-function-ref iadd 2)
         (((@builtin-function-ref iadd 2)
           ((@builtin-function-ref random-dice 0)))
          1))
        1))
      1))
    1)))

;;; LiftLambdaPass

(define (main)
  ((@builtin-function-ref println 1)
   (((@builtin-function-ref iadd 2)
     (((@builtin-function-ref iadd 2)
       (((@builtin-function-ref iadd 2)
         (((@builtin-function-ref iadd 2)
           ((@builtin-function-ref random-dice 0)))
          1))
        1))
      1))
    1)))

