;;; Input

(define (main) (println (isub 50 8)))

;;; ShrinkPass

(define (main) (println (isub 50 8)))

;;; UniquifyPass

(define (main) (println (isub 50 8)))

;;; RevealGlobalPass

(define (main) (println (isub 50 8)))

;;; LiftLambdaPass

(define (main) (println (isub 50 8)))

;;; UnnestOperandPass

(define (main) (let ((_₁ (isub 50 8))) (println _₁)))

