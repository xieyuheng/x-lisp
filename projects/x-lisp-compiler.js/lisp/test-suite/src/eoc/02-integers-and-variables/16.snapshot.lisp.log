;;; Input

(define (main) (println (isub 50 8)))

;;; ShrinkPass

(define (main) (println (isub 50 8)))

;;; UniquifyPass

(define (main) (println (isub 50 8)))

;;; RevealGlobalPass

(define (main) ((@primitive println 1) ((@primitive isub 2) 50 8)))

;;; LiftLambdaPass

(define (main) ((@primitive println 1) ((@primitive isub 2) 50 8)))

;;; UnnestOperandPass

(define (main)
  (@let1 _₁ ((@primitive isub 2) 50 8) ((@primitive println 1) _₁)))

