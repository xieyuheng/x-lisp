;;; Input

(define (main) (= x 4) (println (iadd 8 x)))

;;; Loaded

(define (main) (= x 4) (println (iadd 8 x)))

;;; ShrinkPass

(define (main) (= x 4) (println (iadd 8 x)))

;;; UniquifyPass

(define (main) (= x₁ 4) (println (iadd 8 x₁)))

;;; RevealGlobalPass

(define (main) (= x₁ 4) (println (iadd 8 x₁)))

;;; LiftLambdaPass

(define (main) (= x₁ 4) (println (iadd 8 x₁)))

;;; UnnestOperandPass

(define (main) (= x₁ 4) (= _₁ (iadd 8 x₁)) (println _₁))

