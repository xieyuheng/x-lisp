;;; Input

(define (main) (println (ineg (random-dice))))

;;; ShrinkPass

(define (main) (println (ineg (random-dice))))

;;; UniquifyPass

(define (main) (println (ineg (random-dice))))

;;; RevealGlobalPass

(define (main) (println (ineg (random-dice))))

;;; LiftLambdaPass

(define (main) (println (ineg (random-dice))))

;;; UnnestOperandPass

(define (main)
  (= _₁ (random-dice))
  (= _₂ (ineg _₁))
  (println _₂))

