;;; Input

(define (main) (println (ineg (random-dice))))

;;; ShrinkPass

(define (main) (println (ineg (random-dice))))

;;; UniquifyPass

(define (main) (println (ineg (random-dice))))

;;; RevealGlobalPass

(define (main)
  ((@primitive-ref println 1)
   ((@primitive-ref ineg 1) ((@primitive-ref random-dice 0)))))

;;; LiftLambdaPass

(define (main)
  ((@primitive-ref println 1)
   ((@primitive-ref ineg 1) ((@primitive-ref random-dice 0)))))

