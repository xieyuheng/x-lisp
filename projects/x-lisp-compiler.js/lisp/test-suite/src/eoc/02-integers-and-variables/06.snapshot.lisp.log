;;; Input

(define (main) (println (ineg (random-dice))))

;;; ShrinkPass

(define (main) (println (ineg (random-dice))))

;;; UniquifyPass

(define (main) (println (ineg (random-dice))))

;;; RevealGlobalPass

(define (main)
  ((@primitive println 1) ((@primitive ineg 1) ((@primitive random-dice 0)))))

;;; LiftLambdaPass

(define (main)
  ((@primitive println 1) ((@primitive ineg 1) ((@primitive random-dice 0)))))

;;; UnnestOperandPass

(define (main)
  (@let1 _₁ ((@primitive random-dice 0))
    (@let1 _₂ ((@primitive ineg 1) _₁) ((@primitive println 1) _₂))))

