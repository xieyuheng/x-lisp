;;; Input

(define (main) (println (ineg (random-dice))))

;;; ShrinkPass

(define (main) (println (ineg (random-dice))))

;;; UniquifyPass

(define (main) (println (ineg (random-dice))))

;;; RevealGlobalPass

(define (main)
  ((@builtin-function-ref println 1)
   ((@builtin-function-ref ineg 1)
    ((@builtin-function-ref random-dice 0)))))

;;; LiftLambdaPass

(define (main)
  ((@builtin-function-ref println 1)
   ((@builtin-function-ref ineg 1)
    ((@builtin-function-ref random-dice 0)))))

