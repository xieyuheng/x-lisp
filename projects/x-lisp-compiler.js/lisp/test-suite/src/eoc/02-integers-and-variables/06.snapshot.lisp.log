;;; Input

(define (main) (println (ineg (random-dice))))

;;; ShrinkPass

(define (main) (println (ineg (random-dice))))

;;; UniquifyPass

(define (main) (println (ineg (random-dice))))

;;; RevealGlobalPass

(define (main)
  ((@primitive-function-ref println 1)
   ((@primitive-function-ref ineg 1)
    ((@primitive-function-ref random-dice 0)))))

;;; LiftLambdaPass

(define (main)
  ((@primitive-function-ref println 1)
   ((@primitive-function-ref ineg 1)
    ((@primitive-function-ref random-dice 0)))))

