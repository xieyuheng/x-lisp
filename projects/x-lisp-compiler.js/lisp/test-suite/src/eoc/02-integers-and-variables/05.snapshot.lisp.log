;;; Input

(define (main) (= a 42) (= b a) (println b))

;;; ShrinkPass

(define (main) (let ((a 42)) (let ((b a)) (println b))))

;;; UniquifyPass

(define (main) (let ((a₁ 42)) (let ((b₁ a₁)) (println b₁))))

;;; RevealGlobalPass

(define (main)
  (let ((a₁ 42)) (let ((b₁ a₁)) ((@primitive println 1) b₁))))

;;; LiftLambdaPass

(define (main)
  (let ((a₁ 42)) (let ((b₁ a₁)) ((@primitive println 1) b₁))))

;;; UnnestOperandPass

(define (main)
  (let ((a₁ 42)) (let ((b₁ a₁)) ((@primitive println 1) b₁))))

