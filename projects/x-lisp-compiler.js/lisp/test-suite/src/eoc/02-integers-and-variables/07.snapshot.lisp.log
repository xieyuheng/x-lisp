;;; Input

(define (main) (println (iadd (random-dice) (random-dice))))

;;; ShrinkPass

(define (main) (println (iadd (random-dice) (random-dice))))

;;; UniquifyPass

(define (main) (println (iadd (random-dice) (random-dice))))

;;; RevealGlobalPass

(define (main)
  ((@primitive println 1)
   ((@primitive iadd 2)
    ((@primitive random-dice 0))
    ((@primitive random-dice 0)))))

;;; LiftLambdaPass

(define (main)
  ((@primitive println 1)
   ((@primitive iadd 2)
    ((@primitive random-dice 0))
    ((@primitive random-dice 0)))))

;;; UnnestOperandPass

(define (main)
  (let ((_₁ ((@primitive random-dice 0))))
    (let ((_₂ ((@primitive random-dice 0))))
      (let ((_₃ ((@primitive iadd 2) _₁ _₂)))
        ((@primitive println 1) _₃)))))

