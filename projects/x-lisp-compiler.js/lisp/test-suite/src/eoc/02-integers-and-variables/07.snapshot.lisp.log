;;; Input

(define (main) (println (iadd (random-dice) (random-dice))))

;;; ShrinkPass

(define (main) (println (iadd (random-dice) (random-dice))))

;;; UniquifyPass

(define (main) (println (iadd (random-dice) (random-dice))))

;;; RevealGlobalPass

(define (main) (println (iadd (random-dice) (random-dice))))

;;; LiftLambdaPass

(define (main) (println (iadd (random-dice) (random-dice))))

;;; UnnestOperandPass

(define (main)
  (= _₁ (random-dice))
  (= _₂ (random-dice))
  (= _₃ (iadd _₁ _₂))
  (println _₃))

