;;; Input

(define (main) (println (iadd (random-dice) (random-dice))))

;;; ShrinkPass

(define (main) (println (iadd (random-dice) (random-dice))))

;;; UniquifyPass

(define (main) (println (iadd (random-dice) (random-dice))))

;;; RevealGlobalPass

(define (main) (println (iadd (random-dice) (random-dice))))

;;; LiftLambdaPass

(define (main) (println (iadd (random-dice) (random-dice))))

;;; UnnestOperandPass

(define (main)
  (let ((_₁ (random-dice)))
    (let ((_₂ (random-dice)))
      (let ((_₃ (iadd _₁ _₂))) (println _₃)))))

