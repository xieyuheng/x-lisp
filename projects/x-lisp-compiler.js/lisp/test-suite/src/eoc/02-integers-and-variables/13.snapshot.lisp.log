;;; Input

(define (main)
  (= x (iadd 42 (ineg 10)))
  (println (iadd x 10)))

;;; Loaded

(define (main)
  (= x (iadd 42 (ineg 10)))
  (println (iadd x 10)))

;;; ShrinkPass

(define (main)
  (= x (iadd 42 (ineg 10)))
  (println (iadd x 10)))

;;; UniquifyPass

(define (main)
  (= x₁ (iadd 42 (ineg 10)))
  (println (iadd x₁ 10)))

;;; RevealGlobalPass

(define (main)
  (= x₁ (iadd 42 (ineg 10)))
  (println (iadd x₁ 10)))

;;; LiftLambdaPass

(define (main)
  (= x₁ (iadd 42 (ineg 10)))
  (println (iadd x₁ 10)))

;;; UnnestOperandPass

(define (main)
  (= x₁ (begin (= _₁ (ineg 10)) (iadd 42 _₁)))
  (= _₂ (iadd x₁ 10))
  (println _₂))

