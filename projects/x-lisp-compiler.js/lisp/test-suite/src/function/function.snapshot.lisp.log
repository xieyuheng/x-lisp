;;; Input

(define (main) (println iadd) (println (iadd 1)))

;;; ShrinkPass

(define (main) (println iadd) (println (iadd 1)))

;;; UniquifyPass

(define (main) (println iadd) (println (iadd 1)))

;;; RevealGlobalPass

(define (main) (println iadd) (println (iadd 1)))

;;; LiftLambdaPass

(define (main) (println iadd) (println (iadd 1)))

;;; UnnestOperandPass

(define (main) (println iadd) (= _₁ (iadd 1)) (println _₁))

