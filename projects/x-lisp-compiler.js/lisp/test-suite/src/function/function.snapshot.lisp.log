;;; Input

(define (main) (println iadd) (println (iadd 1)))

;;; ShrinkPass

(define (main) (begin (println iadd) (println (iadd 1))))

;;; UniquifyPass

(define (main) (begin (println iadd) (println (iadd 1))))

;;; RevealGlobalPass

(define (main)
  (begin
    ((@primitive println 1) (@primitive iadd 2))
    ((@primitive println 1) ((@primitive iadd 2) 1))))

;;; LiftLambdaPass

(define (main)
  (begin
    ((@primitive println 1) (@primitive iadd 2))
    ((@primitive println 1) ((@primitive iadd 2) 1))))

;;; UnnestOperandPass

(define (main)
  (begin
    ((@primitive println 1) (@primitive iadd 2))
    (let ((_₁ ((@primitive iadd 2) 1)))
      ((@primitive println 1) _₁))))

