(define-function (swap f x y) (block entry))

(define-function (drop f) (block entry))

(define-function (drop©λ₁ f dropped₁) (block entry))

(define-function (drop©λ₁©λ₁ f x₁) (block entry))

(define-function (dup f) (block entry))

(define-function (dup©λ₁ f x₁) (block entry))

(define-function (identity x) (block entry))

(define-function (main) (block entry))
