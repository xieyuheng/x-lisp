;;; Input

(define (main) (println true) (println false))

;;; ShrinkPass

(define (main) (begin (println true) (println false)))

;;; UniquifyPass

(define (main) (begin (println true) (println false)))

;;; RevealGlobalPass

(define (main)
  (begin
    ((@primitive println 1) (@constant true))
    ((@primitive println 1) (@constant false))))

;;; LiftLambdaPass

(define (main)
  (begin
    ((@primitive println 1) (@constant true))
    ((@primitive println 1) (@constant false))))

;;; UnnestOperandPass

(define (main)
  (begin
    (let ((_₁ (@constant true))) ((@primitive println 1) _₁))
    (let ((_₂ (@constant false))) ((@primitive println 1) _₂))))

