;;; Input

(define (main) (println true) (println false))

;;; ShrinkPass

(define (main) (begin (println true) (println false)))

;;; UniquifyPass

(define (main) (begin (println true) (println false)))

;;; RevealGlobalPass

(define (main) (begin (println true) (println false)))

;;; LiftLambdaPass

(define (main) (begin (println true) (println false)))

;;; UnnestOperandPass

(define (main)
  (begin
    (let ((_₁ true)) (println _₁))
    (let ((_₂ false)) (println _₂))))

