;;; Input

(define (main) (println true) (println false))

;;; ShrinkPass

(define (main) (@begin1 (println true) (println false)))

;;; UniquifyPass

(define (main) (@begin1 (println true) (println false)))

;;; RevealGlobalPass

(define (main)
  (@begin1 ((@primitive println 1) (@constant true))
    ((@primitive println 1) (@constant false))))

;;; LiftLambdaPass

(define (main)
  (@begin1 ((@primitive println 1) (@constant true))
    ((@primitive println 1) (@constant false))))

;;; UnnestOperandPass

(define (main)
  (@begin1 (@let1 _₁ (@constant true) ((@primitive println 1) _₁))
    (@let1 _₂ (@constant false) ((@primitive println 1) _₂))))

