(define-function (main)
  (block entry (test (equal? 1 2)) (branch then₃ else₄))
  (block then₁ (return 2))
  (block else₂ (return (assert #f)))
  (block then₃ (return 1))
  (block else₄ (test (equal? 1 2)) (branch then₁ else₂)))
