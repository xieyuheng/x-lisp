;;; Input

(define (main) (println null))

;;; ShrinkPass

(define (main) (println null))

;;; UniquifyPass

(define (main) (println null))

;;; RevealGlobalPass

(define (main)
  ((@primitive-function-ref println 1) (@primitive-constant-ref null)))

;;; LiftLambdaPass

(define (main)
  ((@primitive-function-ref println 1) (@primitive-constant-ref null)))

;;; UnnestOperandPass

(define (main)
  (@let1 _₁ (@primitive-constant-ref null)
    ((@primitive-function-ref println 1) _₁)))

