;;; Input

(define one 1)

(define two (iadd 1 one))

(define three (iadd 1 two))

(define (main) (println three))

;;; ShrinkPass

(define one 1)

(define two (iadd 1 one))

(define three (iadd 1 two))

(define (main) (println three))

;;; UniquifyPass

(define one 1)

(define two (iadd 1 one))

(define three (iadd 1 two))

(define (main) (println three))

;;; RevealGlobalPass

(define one 1)

(define two (iadd 1 one))

(define three (iadd 1 two))

(define (main) (println three))

;;; LiftLambdaPass

(define one 1)

(define two (iadd 1 one))

(define three (iadd 1 two))

(define (main) (println three))

;;; UnnestOperandPass

(define one 1)

(define two (let ((_₁ one)) (iadd 1 _₁)))

(define three (let ((_₁ two)) (iadd 1 _₁)))

(define (main) (let ((_₁ three)) (println _₁)))

