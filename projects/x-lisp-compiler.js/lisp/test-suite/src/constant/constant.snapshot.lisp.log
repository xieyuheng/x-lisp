;;; Input

(claim one int-t)

(define one 1)

(claim two int-t)

(define two (iadd 1 one))

(claim three int-t)

(define three (iadd 1 two))

(claim main (-> void-t))

(define (main) (println three))

;;; Loaded

(define one 1)

(define two (iadd 1 one))

(define three (iadd 1 two))

(define (main) (println three))

;;; ShrinkPass

(define one 1)

(define two (iadd 1 one))

(define three (iadd 1 two))

(define (main) (println three))

;;; UniquifyPass

(define one 1)

(define two (iadd 1 one))

(define three (iadd 1 two))

(define (main) (println three))

;;; LiftLambdaPass

(define one 1)

(define two (iadd 1 one))

(define three (iadd 1 two))

(define (main) (println three))

;;; UnnestOperandPass

(define one 1)

(define two (iadd 1 one))

(define three (iadd 1 two))

(define (main) (println three))

