(define-variable one (block body (return 1)))

(define-variable two
  (block body (= _₁ one) (return (iadd 1 _₁))))

(define-variable three
  (block body (= _₁ two) (return (iadd 1 _₁))))

(define-function (main)
  (block body (= _₁ three) (return (println _₁))))
