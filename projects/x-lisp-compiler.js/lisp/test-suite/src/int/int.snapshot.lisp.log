;;; Input

(define (square x) (imul x x))

(define (main) (println (square 3)))

;;; ShrinkPass

(define (square x) (imul x x))

(define (main) (println (square 3)))

;;; UniquifyPass

(define (square x) (imul x x))

(define (main) (println (square 3)))

;;; RevealGlobalPass

(define (square x) ((@primitive-function-ref imul 2) x x))

(define (main)
  ((@primitive-function-ref println 1)
   ((@function-ref square 1) 3)))

;;; LiftLambdaPass

(define (square x) ((@primitive-function-ref imul 2) x x))

(define (main)
  ((@primitive-function-ref println 1)
   ((@function-ref square 1) 3)))

;;; UnnestOperandPass

(define (square x) ((@primitive-function-ref imul 2) x x))

(define (main)
  (@let1 _₁ ((@function-ref square 1) 3)
    ((@primitive-function-ref println 1) _₁)))

