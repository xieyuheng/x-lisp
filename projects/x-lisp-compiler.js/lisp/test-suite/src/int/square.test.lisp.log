;;; Input

(import "square" square)

(define (main) (assert-equal 81 (square (square 3))))

(define (square x) (imul x x))

;;; ShrinkPass

(import "square" square)

(define (main) (assert-equal 81 (square (square 3))))

(define (square x) (imul x x))

;;; UniquifyPass

(import "square" square)

(define (main) (assert-equal 81 (square (square 3))))

(define (square x) (imul x x))

;;; RevealGlobalPass

(import "square" square)

(define (main)
  ((@primitive-function-ref assert-equal 2)
   81
   ((@function-ref square 1) ((@function-ref square 1) 3))))

(define (square x) (imul x x))

;;; LiftLambdaPass

(import "square" square)

(define (main)
  ((@primitive-function-ref assert-equal 2)
   81
   ((@function-ref square 1) ((@function-ref square 1) 3))))

;;; UnnestOperandPass

(import "square" square)

(define (main)
  (@let1 _₁ ((@function-ref square 1) 3)
    (@let1 _₂ ((@function-ref square 1) _₁)
      ((@primitive-function-ref assert-equal 2) 81 _₂))))

