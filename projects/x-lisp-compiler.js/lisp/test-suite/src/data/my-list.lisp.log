;;; Input

(export nil li)

(define nil #nil)

(define (li head tail) [#li head tail])

;;; ShrinkPass

(export nil li)

(define nil #nil)

(define (li head tail)
  (let ((tael (make-list)))
    (begin
      (list-push! #li tael)
      (begin
        (list-push! head tael)
        (begin (list-push! tail tael) tael)))))

;;; UniquifyPass

(export nil li)

(define nil #nil)

(define (li head tail)
  (let ((tael₁ (make-list)))
    (begin
      (list-push! #li tael₁)
      (begin
        (list-push! head tael₁)
        (begin (list-push! tail tael₁) tael₁)))))

;;; RevealGlobalPass

(export nil li)

(define nil #nil)

(define (li head tail)
  (let ((tael₁ (make-list)))
    (begin
      (list-push! #li tael₁)
      (begin
        (list-push! head tael₁)
        (begin (list-push! tail tael₁) tael₁)))))

;;; LiftLambdaPass

(export nil li)

(define nil #nil)

(define (li head tail)
  (let ((tael₁ (make-list)))
    (begin
      (list-push! #li tael₁)
      (begin
        (list-push! head tael₁)
        (begin (list-push! tail tael₁) tael₁)))))

;;; UnnestOperandPass

(export nil li)

(define nil #nil)

(define (li head tail)
  (let ((tael₁ (make-list)))
    (begin
      (list-push! #li tael₁)
      (begin
        (list-push! head tael₁)
        (begin (list-push! tail tael₁) tael₁)))))

