;;; Input

(define (main) (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; ShrinkPass

(define (main) (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; UniquifyPass

(define (main) (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; RevealGlobalPass

(define (main)
  ((@primitive assert-equal 2)
   'abcdef
   ((@primitive symbol-append 2) 'abc 'def)))

;;; LiftLambdaPass

(define (main)
  ((@primitive assert-equal 2)
   'abcdef
   ((@primitive symbol-append 2) 'abc 'def)))

;;; UnnestOperandPass

(define (main)
  (let ((_₁ ((@primitive symbol-append 2) 'abc 'def)))
    ((@primitive assert-equal 2) 'abcdef _₁)))

