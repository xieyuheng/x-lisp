;;; Input

(define (main) (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; ShrinkPass

(define (main) (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; UniquifyPass

(define (main) (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; RevealGlobalPass

(define (main)
  ((@primitive-function-ref assert-equal 2)
   'abcdef
   ((@primitive-function-ref symbol-append 2) 'abc 'def)))

;;; LiftLambdaPass

(define (main)
  ((@primitive-function-ref assert-equal 2)
   'abcdef
   ((@primitive-function-ref symbol-append 2) 'abc 'def)))

;;; UnnestOperandPass

(define (main)
  (@let1 _₁ ((@primitive-function-ref symbol-append 2) 'abc 'def)
    ((@primitive-function-ref assert-equal 2) 'abcdef _₁)))

