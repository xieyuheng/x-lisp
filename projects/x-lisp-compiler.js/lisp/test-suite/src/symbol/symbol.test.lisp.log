;;; Input

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; Loaded

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; ShrinkPass

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; UniquifyPass

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; LiftLambdaPass

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; UnnestOperandPass

(define (main)
  (= _₁ (symbol-append 'abc 'def))
  (assert-equal 'abcdef _₁))

