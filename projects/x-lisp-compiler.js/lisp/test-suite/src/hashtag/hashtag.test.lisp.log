;;; Input

(define (main) (assert-equal #abcdef (hashtag-append #abc #def)))

;;; ShrinkPass

(define (main) (assert-equal #abcdef (hashtag-append #abc #def)))

;;; UniquifyPass

(define (main) (assert-equal #abcdef (hashtag-append #abc #def)))

;;; RevealGlobalPass

(define (main)
  ((@primitive-function-ref assert-equal 2)
   #abcdef
   ((@primitive-function-ref hashtag-append 2) #abc #def)))

;;; LiftLambdaPass

(define (main)
  ((@primitive-function-ref assert-equal 2)
   #abcdef
   ((@primitive-function-ref hashtag-append 2) #abc #def)))

;;; UnnestOperandPass

(define (main)
  (@let1 _₁ ((@primitive-function-ref hashtag-append 2) #abc #def)
    ((@primitive-function-ref assert-equal 2) #abcdef _₁)))

