;;; Input

(define (main)
  (assert-equal #abcdef (hashtag-append #abc #def)))

;;; ShrinkPass

(define (main)
  (assert-equal #abcdef (hashtag-append #abc #def)))

;;; UniquifyPass

(define (main)
  (assert-equal #abcdef (hashtag-append #abc #def)))

;;; RevealGlobalPass

(define (main)
  (assert-equal #abcdef (hashtag-append #abc #def)))

;;; LiftLambdaPass

(define (main)
  (assert-equal #abcdef (hashtag-append #abc #def)))

;;; UnnestOperandPass

(define (main)
  (let ((_₁ (hashtag-append #abc #def)))
    (assert-equal #abcdef _₁)))

