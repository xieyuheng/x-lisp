;;; Input

(define (main)
  (assert-equal "abcdef" (string-append "abc" "def")))

;;; ShrinkPass

(define (main)
  (assert-equal "abcdef" (string-append "abc" "def")))

;;; UniquifyPass

(define (main)
  (assert-equal "abcdef" (string-append "abc" "def")))

;;; RevealGlobalPass

(define (main)
  ((@primitive assert-equal 2)
   "abcdef"
   ((@primitive string-append 2) "abc" "def")))

;;; LiftLambdaPass

(define (main)
  ((@primitive assert-equal 2)
   "abcdef"
   ((@primitive string-append 2) "abc" "def")))

;;; UnnestOperandPass

(define (main)
  (let ((_₁ ((@primitive string-append 2) "abc" "def")))
    ((@primitive assert-equal 2) "abcdef" _₁)))

