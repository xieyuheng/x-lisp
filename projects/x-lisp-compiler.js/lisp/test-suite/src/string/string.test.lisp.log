;;; Input

(define (main)
  (assert-equal "abcdef" (string-append "abc" "def")))

;;; Loaded

(define (main)
  (assert-equal "abcdef" (string-append "abc" "def")))

;;; ShrinkPass

(define (main)
  (assert-equal "abcdef" (string-append "abc" "def")))

;;; UniquifyPass

(define (main)
  (assert-equal "abcdef" (string-append "abc" "def")))

;;; LiftLambdaPass

(define (main)
  (assert-equal "abcdef" (string-append "abc" "def")))

;;; UnnestOperandPass

(define (main)
  (= _₁ (string-append "abc" "def"))
  (assert-equal "abcdef" _₁))

