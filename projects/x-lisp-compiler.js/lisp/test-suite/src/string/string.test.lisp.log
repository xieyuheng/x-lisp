;;; Input

(define (main)
  (assert-equal "abcdef" (string-append "abc" "def")))

;;; ShrinkPass

(define (main)
  (assert-equal "abcdef" (string-append "abc" "def")))

;;; UniquifyPass

(define (main)
  (assert-equal "abcdef" (string-append "abc" "def")))

;;; RevealGlobalPass

(define (main)
  (assert-equal
    "abcdef"
    ((@primitive-function-ref string-append 2) "abc" "def")))

;;; LiftLambdaPass

(define (main)
  (assert-equal
    "abcdef"
    ((@primitive-function-ref string-append 2) "abc" "def")))

