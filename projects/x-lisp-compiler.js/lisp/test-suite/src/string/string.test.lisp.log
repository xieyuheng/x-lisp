;;; Input

(define (main) (assert-equal "abcdef" (string-append "abc" "def")))

;;; ShrinkPass

(define (main) (assert-equal "abcdef" (string-append "abc" "def")))

;;; UniquifyPass

(define (main) (assert-equal "abcdef" (string-append "abc" "def")))

;;; RevealGlobalPass

(define (main)
  ((@primitive-function-ref assert-equal 2)
   "abcdef"
   ((@primitive-function-ref string-append 2) "abc" "def")))

;;; LiftLambdaPass

(define (main)
  ((@primitive-function-ref assert-equal 2)
   "abcdef"
   ((@primitive-function-ref string-append 2) "abc" "def")))

;;; UnnestOperandPass

(define (main)
  (@let1 _₁ ((@primitive-function-ref string-append 2) "abc" "def")
    ((@primitive-function-ref assert-equal 2) "abcdef" _₁)))

