(type-error-module l1-l2-type-error)

(define (evaluate-and-print exp)
  (println (l1/evaluate exp (l1/empty-env))))

(define (main)
  ;; l1 -- ok
  (evaluate-and-print (l1/lambda-exp 'x (l1/var-exp 'x)))
  ;; l2 -- error
  (evaluate-and-print (l2/lambda-exp 'x (l2/var-exp 'x))))
