(define-function _main
  (block body
    (= _₁ (literal (@primitive-function println-non-void 1)))
    (= _₄ (literal (@primitive-function equal? 2)))
    (= _₅ (literal (@primitive-function random-dice 0)))
    (= _₆ (apply-nullary _₅))
    (= _₇ (apply _₄ _₆))
    (= _₈ (literal 1))
    (= _₉ (apply _₇ _₈))
    (branch _₉ _main/then₅ _main/else₆))
  (block _main/let-body₁ (= _↩ (apply _₁ _₂)) (return _↩))
  (block _main/then₂
    (= _₂ (literal 0))
    (goto _main/let-body₁))
  (block _main/else₃
    (= _₂ (literal 42))
    (goto _main/let-body₁))
  (block _main/let-body₄
    (branch _₃ _main/then₂ _main/else₃))
  (block _main/then₅
    (= _₁₀ (literal (@primitive-function equal? 2)))
    (= _₁₁ (literal (@primitive-function random-dice 0)))
    (= _₁₂ (apply-nullary _₁₁))
    (= _₁₃ (apply _₁₀ _₁₂))
    (= _₁₄ (literal 2))
    (= _₃ (apply _₁₃ _₁₄))
    (goto _main/let-body₄))
  (block _main/else₆
    (= _₃ (literal #f))
    (goto _main/let-body₄)))

(define-metadata _main©metadata
  :name "_main"
  :arity 0
  :is-primitive 0
  :variable-info _main©variable-info)

(define-metadata _main©variable-info)

(define-variable _main©constant)

(define-setup _main©setup
  (block body
    (= address (literal (@address _main)))
    (= metadata (literal (@address _main©metadata)))
    (= function
       (call
        (@primitive-function make-function 2)
        address
        metadata))
    (store _main©constant function)
    (return)))

(define-metadata equal?©metadata
  :name "equal?"
  :arity 2
  :is-primitive 1
  :variable-info 0)

(define-variable equal?©constant)

(define-setup equal?©setup
  (block body
    (= address (literal (@primitive-address equal?)))
    (= metadata (literal (@address equal?©metadata)))
    (= function
       (call
        (@primitive-function make-function 2)
        address
        metadata))
    (store equal?©constant function)
    (return)))

(define-metadata println-non-void©metadata
  :name "println-non-void"
  :arity 1
  :is-primitive 1
  :variable-info 0)

(define-variable println-non-void©constant)

(define-setup println-non-void©setup
  (block body
    (= address
       (literal (@primitive-address println-non-void)))
    (= metadata
       (literal (@address println-non-void©metadata)))
    (= function
       (call
        (@primitive-function make-function 2)
        address
        metadata))
    (store println-non-void©constant function)
    (return)))

(define-metadata make-function©metadata
  :name "make-function"
  :arity 2
  :is-primitive 1
  :variable-info 0)

(define-variable make-function©constant)

(define-setup make-function©setup
  (block body
    (= address (literal (@primitive-address make-function)))
    (= metadata (literal (@address make-function©metadata)))
    (= function
       (call
        (@primitive-function make-function 2)
        address
        metadata))
    (store make-function©constant function)
    (return)))

(define-metadata random-dice©metadata
  :name "random-dice"
  :arity 0
  :is-primitive 1
  :variable-info 0)

(define-variable random-dice©constant)

(define-setup random-dice©setup
  (block body
    (= address (literal (@primitive-address random-dice)))
    (= metadata (literal (@address random-dice©metadata)))
    (= function
       (call
        (@primitive-function make-function 2)
        address
        metadata))
    (store random-dice©constant function)
    (return)))
