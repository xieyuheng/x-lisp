(define-function _main
  (block body
    (= _₁ (literal (@primitive-function println-non-void 1)))
    (= _₂ (literal (@primitive-function random-dice 0)))
    (= x₁ (apply-nullary _₂))
    (= _₃ (literal (@primitive-function random-dice 0)))
    (= y₁ (apply-nullary _₃))
    (= _₆ (literal (@primitive-function int-less? 2)))
    (= _₇ (apply _₆ x₁))
    (= _₈ (literal 1))
    (= _₉ (apply _₇ _₈))
    (branch _₉ _main/then₅ _main/else₆))
  (block _main/let-body₁ (= _↩ (apply _₁ _₄)) (return _↩))
  (block _main/then₂
    (= _₁₆ (literal (@primitive-function iadd 2)))
    (= _₁₇ (apply _₁₆ y₁))
    (= _₁₈ (literal 2))
    (= _₄ (apply _₁₇ _₁₈))
    (goto _main/let-body₁))
  (block _main/else₃
    (= _₁₉ (literal (@primitive-function iadd 2)))
    (= _₂₀ (apply _₁₉ y₁))
    (= _₂₁ (literal 10))
    (= _₄ (apply _₂₀ _₂₁))
    (goto _main/let-body₁))
  (block _main/let-body₄
    (branch _₅ _main/then₂ _main/else₃))
  (block _main/then₅
    (= _₁₀ (literal (@primitive-function equal? 2)))
    (= _₁₁ (apply _₁₀ x₁))
    (= _₁₂ (literal 0))
    (= _₅ (apply _₁₁ _₁₂))
    (goto _main/let-body₄))
  (block _main/else₆
    (= _₁₃ (literal (@primitive-function equal? 2)))
    (= _₁₄ (apply _₁₃ x₁))
    (= _₁₅ (literal 2))
    (= _₅ (apply _₁₄ _₁₅))
    (goto _main/let-body₄)))

(define-metadata _main©metadata
  :name "_main"
  :arity 0
  :is-primitive 0)

(define-variable _main©constant)

(define-setup _main©setup
  (block body
    (= address (literal (@address _main)))
    (= arity (literal 0))
    (= size (literal 0))
    (= curry
       (call
        (@primitive-function make-curry 3)
        address
        arity
        size))
    (store _main©constant curry)
    (return)))

(define-metadata equal?©metadata
  :name "equal?"
  :arity 2
  :is-primitive 1)

(define-variable equal?©constant)

(define-setup equal?©setup
  (block body
    (= address (literal (@primitive-address equal?)))
    (= arity (literal 2))
    (= size (literal 0))
    (= curry
       (call
        (@primitive-function make-curry 3)
        address
        arity
        size))
    (store equal?©constant curry)
    (return)))

(define-metadata println-non-void©metadata
  :name "println-non-void"
  :arity 1
  :is-primitive 1)

(define-variable println-non-void©constant)

(define-setup println-non-void©setup
  (block body
    (= address
       (literal (@primitive-address println-non-void)))
    (= arity (literal 1))
    (= size (literal 0))
    (= curry
       (call
        (@primitive-function make-curry 3)
        address
        arity
        size))
    (store println-non-void©constant curry)
    (return)))

(define-metadata iadd©metadata
  :name "iadd"
  :arity 2
  :is-primitive 1)

(define-variable iadd©constant)

(define-setup iadd©setup
  (block body
    (= address (literal (@primitive-address iadd)))
    (= arity (literal 2))
    (= size (literal 0))
    (= curry
       (call
        (@primitive-function make-curry 3)
        address
        arity
        size))
    (store iadd©constant curry)
    (return)))

(define-metadata int-less?©metadata
  :name "int-less?"
  :arity 2
  :is-primitive 1)

(define-variable int-less?©constant)

(define-setup int-less?©setup
  (block body
    (= address (literal (@primitive-address int-less?)))
    (= arity (literal 2))
    (= size (literal 0))
    (= curry
       (call
        (@primitive-function make-curry 3)
        address
        arity
        size))
    (store int-less?©constant curry)
    (return)))

(define-metadata make-curry©metadata
  :name "make-curry"
  :arity 3
  :is-primitive 1)

(define-variable make-curry©constant)

(define-setup make-curry©setup
  (block body
    (= address (literal (@primitive-address make-curry)))
    (= arity (literal 3))
    (= size (literal 0))
    (= curry
       (call
        (@primitive-function make-curry 3)
        address
        arity
        size))
    (store make-curry©constant curry)
    (return)))

(define-metadata random-dice©metadata
  :name "random-dice"
  :arity 0
  :is-primitive 1)

(define-variable random-dice©constant)

(define-setup random-dice©setup
  (block body
    (= address (literal (@primitive-address random-dice)))
    (= arity (literal 0))
    (= size (literal 0))
    (= curry
       (call
        (@primitive-function make-curry 3)
        address
        arity
        size))
    (store random-dice©constant curry)
    (return)))
