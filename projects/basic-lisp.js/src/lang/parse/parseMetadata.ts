import * as S from "@xieyuheng/sexp.js"
import type { Metadata } from "../metadata/index.ts"

export function parseMetadataAttributes(sexp: S.Sexp): Record<string, Metadata> {
  throw new Error("TODO")
}
