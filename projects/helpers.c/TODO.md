[helpers.c] `record_first_value` & `record_next_value`

```c
// - iterate by hashed order (NOT insertion order).
void *record_first_value(record_t *self);
void *record_next_value(record_t *self);
```

