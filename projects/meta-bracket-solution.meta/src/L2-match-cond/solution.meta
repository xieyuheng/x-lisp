(module l2-match-cond)

(define-enum (result-t A)
  (ok (value A))
  (err (msg string-t)))

(define (describe-result x)
  (match x
    ((ok v)
     (cond
       ((int-greater? v 100) "big")
       ((int-less? v 10) "small")
       (else "medium")))
    ((err msg) "error")))
