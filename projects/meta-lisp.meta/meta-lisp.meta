#!/usr/bin/env bash

function call {
    ../stack-lisp.c/src/stack-lisp.exe call build/bundle.stack $@
}

function main {
    local command=$1; shift
    case $command in
        (check)
            local path=${1:-"project.sexp"}
            call meta/check-command $path
            ;;
        (*)
            echo "unknown command: $command"
            exit 1
            ;;
    esac
}

main $@
