(module meta)

(define (check-command config-path dump)
  (= project (load-project (path-resolve config-path)))
  (= options (make-hash))
  (when (equal? dump "true")
    (hash-put! 'dump true options))
  (check-pipeline project options))
