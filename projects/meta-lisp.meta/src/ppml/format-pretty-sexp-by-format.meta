(module ppml)

(claim format-pretty-sexp-by-format
  (polymorphic (A)
    (-> (-> A string-t) sexp-config-t int-t A
        string-t)))

(define (format-pretty-sexp-by-format format config width data)
  (= code (format data))
  (= sexps (parse-sexps "[formatPrettySexpByFormat]" code))
  (string-join "\n\n" (list-map (lambda (sexp) (format-pretty-sexp width sexp config)) sexps)))
