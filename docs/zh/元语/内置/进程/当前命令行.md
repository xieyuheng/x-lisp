---
title: 当前命令行
---

# 类型

```meta-lisp
(-> (列表型 字符串型))
```

# 描述

获取 `--` 分隔符之后的命令行参数。

当 xvm 可执行文件以 `--` 调用时，`--` 之后的所有参数将被收集为当前命令行。
例如：

```
xvm run program.xexe -- check --profile
```

`(当前命令行)` 返回 `["check" "--profile"]`。

# 示例

```meta-lisp
(当前命令行)  ;; => ["check" "--profile"]
```
