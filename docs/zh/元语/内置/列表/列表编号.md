---
title: 列表编号
---

# 类型

```meta-lisp
(多态 (A) (-> (列表型 A) (列表型 (二元组型 整数型 A))))
```

# 描述

将列表中的每个元素与其索引配对。

# 例子

```meta-lisp
(列表编号 [])            ;; => []
(列表编号 ['a])          ;; => [(作二元组 0 'a)]
(列表编号 ['a 'b 'c])    ;; => [(作二元组 0 'a) (作二元组 1 'b) (作二元组 2 'c)]
```
