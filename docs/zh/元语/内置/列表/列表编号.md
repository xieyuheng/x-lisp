---
title: 列表编号
---

# 类型

```meta-lisp
(多态 (甲) (-> (列表型 甲) (列表型 (序对型 整数型 甲))))
```

# 描述

将列表中的每个元素与其索引配对。

# 例子

```meta-lisp
(列表编号 [])            ;; => []
(列表编号 ['a])          ;; => [(作序对 0 'a)]
(列表编号 ['a 'b 'c])    ;; => [(作序对 0 'a) (作序对 1 'b) (作序对 2 'c)]
```
