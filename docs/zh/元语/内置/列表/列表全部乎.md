---
title: 列表全部乎
---

# 类型

```meta-lisp
(多态 (A) (-> (-> A 布尔型) (列表型 A) 布尔型))
```

# 描述

判断列表中是否所有元素都满足条件。空列表返回 `真`。

# 例子

```meta-lisp
(列表全部乎 整数非负乎 [0 1 2 3])  ;; => 真
(列表全部乎 整数非负乎 [0 1 -1])   ;; => 假
```
