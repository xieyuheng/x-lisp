---
title: 列表解拉链
---

# 类型

```meta-lisp
(多态 (甲 乙) (-> (列表型 (二元组型 甲 乙)) (二元组型 (列表型 甲) (列表型 乙))))
```

# 描述

将 pair 列表拆分为两个列表。

# 例子

```meta-lisp
(列表解拉链 [(作二元组 'a 1) (作二元组 'b 2)])  ;; => (作二元组 ['a 'b] [1 2])
```
