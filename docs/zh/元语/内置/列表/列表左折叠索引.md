---
title: 列表左折叠索引
---

# 类型

```meta-lisp
(多态 (甲 乙) (-> (-> 整数型 乙 甲 乙) 乙 (列表型 甲) 乙))
```

# 描述

带索引的左折叠。回调依次接收 index、已折叠值和当前元素。

# 例子

```meta-lisp
(列表左折叠索引 (函 (i folded x) (整数加 folded (整数乘 i x))) 0 [10 20 30])
;; => 80

(列表左折叠索引 (函 (i folded _) (添 i folded)) [] ['a 'b 'c])
;; => [2 1 0]
```
