---
title: 列表存在乎
---

# 类型

```meta-lisp
(多态 (A) (-> (-> A 布尔型) (列表型 A) 布尔型))
```

# 描述

判断列表中是否有元素满足条件。空列表返回 `假`。

# 例子

```meta-lisp
(列表存在乎 整数非负乎 [-1 0 1])  ;; => 真
(列表存在乎 整数非负乎 [-1 -2])   ;; => 假
```
