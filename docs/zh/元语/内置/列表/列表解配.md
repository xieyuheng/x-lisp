---
title: 列表解配
---

# 类型

```meta-lisp
(多态 (A B) (-> (列表型 (二元组型 A B)) (二元组型 (列表型 A) (列表型 B))))
```

# 描述

将 pair 列表拆分为两个列表。

# 例子

```meta-lisp
(列表解配 [(作二元组 'a 1) (作二元组 'b 2)])  ;; => (作二元组 ['a 'b] [1 2])
```
