---
title: 列表选取
---

# 类型

```meta-lisp
(多态 (A) (-> (-> A 布尔型) (列表型 A) (列表型 A)))
```

# 描述

筛选列表中满足条件的元素，保留匹配的。

# 例子

```meta-lisp
(列表选取 整数乎 ['a 1 'b 2])       ;; => [1 2]
(列表选取 整数非负乎 [0 1 -1 2])  ;; => [0 1 2]
```
