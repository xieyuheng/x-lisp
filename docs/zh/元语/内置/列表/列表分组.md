---
title: 列表分组
---

# 类型

```meta-lisp
(多态 (K V) (-> (-> V K) (列表型 V) (散列型 K (列表型 V))))
```

# 描述

按 key 函数对列表进行分组，返回哈希表。

# 例子

```meta-lisp
;; (交换参数 整数模 3) 翻转参数：(交换参数 整数模 3) => (函 (x) (整数模 x 3))
(列表分组 (交换参数 整数模 3) [0 1 2 3 4 5])
;; => (@散列 0 [0 3] 1 [1 4] 2 [2 5])
```
