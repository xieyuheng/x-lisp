---
title: 列表映射索引
---

# 类型

```meta-lisp
(多态 (A B) (-> (-> 整数型 A B) (列表型 A) (列表型 B)))
```

# 描述

对列表中的每个元素应用带索引的函数，返回结果列表。

# 例子

```meta-lisp
(列表映射索引 (函 (i x) (整数加 i x)) [10 20 30])  ;; => [10 21 32]
(列表映射索引 (函 (i _) i) ['a 'b 'c])            ;; => [0 1 2]
```
