---
title: 列表映射
---

# 类型

```meta-lisp
(多态 (A B) (-> (-> A B) (列表型 A) (列表型 B)))
```

# 描述

对列表中的每个元素应用函数，返回结果列表。

# 例子

```meta-lisp
(列表映射 (整数加 10) [1 2 3])  ;; => [11 12 13]
(列表映射 为文本 [1 "a" 3])  ;; => [假 真 假]
```
