---
title: 列表查找
---

# 类型

```meta-lisp
(多态 (A) (-> (-> A 布尔型) (列表型 A) (或有型 A)))
```

# 描述

查找列表中第一个满足条件的元素，返回 `(有 value)` 或 `(无)`。

# 例子

```meta-lisp
(列表查找 整数乎 ['a 'b 3 'd])  ;; => (有 3)
(列表查找 整数乎 ['a 'b 'c])    ;; => (无)
```
