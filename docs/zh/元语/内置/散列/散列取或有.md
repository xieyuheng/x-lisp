---
title: 散列取或有
---

# 类型

```meta-lisp
(多态 (K V) (-> K (散列型 K V) (或有型 V)))
```

# 描述

按键查找，找到时返回 `(有 V)`，未找到时返回 `无`。

# 例子

```meta-lisp
(散列取或有 2 (@散列 1 "a" 2 "b" 3 "c"))  ;; => (有 "b")
(散列取或有 4 (@散列 1 "a" 2 "b" 3 "c"))  ;; => 无
```
