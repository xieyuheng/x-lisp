---
title: 散列逐项
---

# 类型

```meta-lisp
(多态 (K V Any) (-> (-> K V Any) (散列型 K V) 空型))
```

# 描述

遍历每个键值对并执行副作用。

# 例子

```meta-lisp
(散列逐项
  (函 (key value)
    (打印行 key)
    (打印行 value))
  (@散列 1 2 3 4))
```
