---
title: 散列选取
---

# 类型

```meta-lisp
(多态 (键 值) (-> (-> 键 值 真假型) (散列型 键 值) (散列型 键 值)))
```

# 描述

保留谓词返回真的条目。

# 例子

```meta-lisp
(散列选取
  (函 (k v) (整数为非负 v))
  (@散列 'a 1 'b 2 'x -1 'y -2))
;; => (@散列 'a 1 'b 2)
```
