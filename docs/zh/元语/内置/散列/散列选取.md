---
title: 散列选取
---

# 类型

```meta-lisp
(多态 (K V) (-> (-> K V 真假型) (散列型 K V) (散列型 K V)))
```

# 描述

保留谓词返回真的条目。

# 例子

```meta-lisp
(散列选取
  (函 (k v) (整数为非负 v))
  (@散列 'a 1 'b 2 'x -1 'y -2))
;; => (@散列 'a 1 'b 2)
```
