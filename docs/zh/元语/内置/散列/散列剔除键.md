---
title: 散列剔除键
---

# 类型

```meta-lisp
(多态 (K V) (-> (-> K 真假型) (散列型 K V) (散列型 K V)))
```

# 描述

移除键满足谓词的条目。

# 例子

```meta-lisp
(散列剔除键
  整数为非负
  (@散列 1 'a 2 'b -1 'x -2 'y))
;; => (@散列 -1 'x -2 'y)
```
