---
title: 散列映射
---

# 类型

```meta-lisp
(多态 (K1 V1 K2 V2)
  (-> (-> K1 V1 (散列条目型 K2 V2))
      (散列型 K1 V1)
      (散列型 K2 V2)))
```

# 描述

对键和值应用函数，产生新的 entry。

# 例子

```meta-lisp
(散列映射
  (函 (k v) (作散列条目 (整数加 1 k) (整数加 1 v)))
  (@散列 1 2 3 4))
;; => (@散列 2 3 4 5)
```
