---
title: 散列删除之
---

# 类型

```meta-lisp
(多态 (K V) (-> K (散列型 K V) (散列型 K V)))
```

# 描述

删除键值对，返回新的哈希表。

# 例子

```meta-lisp
(令 ((h (@散列 "a" 1 "b" 2 "c" 3)))
  (散列删除之 "a" h))  ;; => (@散列 "b" 2 "c" 3)
```
