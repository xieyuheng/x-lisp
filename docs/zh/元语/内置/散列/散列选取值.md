---
title: 散列选取值
---

# 类型

```meta-lisp
(多态 (K V) (-> (-> V 真假型) (散列型 K V) (散列型 K V)))
```

# 描述

保留值满足谓词的条目。

# 例子

```meta-lisp
(散列选取值
  整数为非负
  (@散列 'a 1 'b 2 'x -1 'y -2))
;; => (@散列 'a 1 'b 2)
```
