---
title: 散列取
---

# 类型

```meta-lisp
(多态 (K V) (-> K (散列型 K V) V))
```

# 描述

根据键获取值。键不存在时报错。

# 例子

```meta-lisp
(散列取 "a" (@散列 "a" 1 "b" 2))  ;; => 1
;; (散列取 "c" (@散列 "a" 1 "b" 2))  ;; 错误：键不存在
```
