---
title: 散列剔除值
---

# 类型

```meta-lisp
(多态 (K V) (-> (-> V 布尔型) (散列型 K V) (散列型 K V)))
```

# 描述

移除值满足谓词的条目。

# 例子

```meta-lisp
(散列剔除值
  整数非负乎
  (@散列 'a 1 'b 2 'x -1 'y -2))
;; => (@散列 'x -1 'y -2)
```
