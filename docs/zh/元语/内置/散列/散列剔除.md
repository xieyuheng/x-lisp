---
title: 散列剔除
---

# 类型

```meta-lisp
(多态 (K V) (-> (-> K V 布尔型) (散列型 K V) (散列型 K V)))
```

# 描述

移除谓词返回真的条目。

# 例子

```meta-lisp
(散列剔除
  (函 (k v) (整数非负乎 v))
  (@散列 'a 1 'b 2 'x -1 'y -2))
;; => (@散列 'x -1 'y -2)
```
