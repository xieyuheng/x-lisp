---
title: 散列映射值
---

# 类型

```meta-lisp
(多态 (K V1 V2) (-> (-> V1 V2) (散列型 K V1) (散列型 K V2)))
```

# 描述

对值应用函数，键保持不变。

# 例子

```meta-lisp
(散列映射值 (函 (n) (整数加 10 n)) (@散列 'a 1 'b 2))
;; => (@散列 'a 11 'b 12)
```
