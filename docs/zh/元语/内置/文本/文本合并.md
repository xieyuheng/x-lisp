---
title: 文本合并
---

# 类型

```meta-lisp
(-> 文本型 (列表型 文本型) 文本型)
```

# 描述

用分隔符连接文本列表，是 `文本分割` 的逆操作。

# 例子

```meta-lisp
(文本合并 "," ["a" "b" "c"])  ;; => "a,b,c"
(文本合并 " " ["a" "b"])      ;; => "a b"
(文本合并 "" ["a" "b"])       ;; => "ab"
```
