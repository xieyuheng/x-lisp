---
title: 文本分割
---

# 类型

```meta-lisp
(-> 文本型 文本型 (列表型 文本型))
```

# 描述

按分隔符将文本拆分为列表。

# 例子

```meta-lisp
(文本分割 "a,b,c" ",")     ;; => ["a" "b" "c"]
(文本分割 "hello" ",")     ;; => ["hello"]
(文本分割 "" ",")          ;; => [""]
```
