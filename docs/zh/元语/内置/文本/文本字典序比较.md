---
title: 文本字典序比较
---

# 类型

```meta-lisp
(-> 文本型 文本型 整数型)
```

# 描述

字典序比较。如果第一个小于第二个返回 `-1`，相等返回 `0`，大于返回 `1`。

# 例子

```meta-lisp
(文本字典序比较 "a" "b")  ;; => -1
(文本字典序比较 "a" "a")  ;; => 0
(文本字典序比较 "b" "a")  ;; => 1
```
