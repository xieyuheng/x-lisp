---
title: 字符串字符
---

# 类型

```meta-lisp
(-> 字符串型 (列表型 字符串型))
```

# 描述

将字符串拆分为单个字符的字符串列表。

# 例子

```meta-lisp
(字符串字符 "abc")   ;; => ["a" "b" "c"]
(字符串字符 "")      ;; => []
(字符串字符 "你好")  ;; => ["你" "好"]
```
