---
title: 字符串结尾
---

# 类型

```meta-lisp
(-> 字符串型 字符串型 真假型)
```

# 描述

判断字符串是否以指定后缀结尾。

# 例子

```meta-lisp
(字符串结尾 "hello" "lo")  ;; => 真
(字符串结尾 "hello" "hi")  ;; => 假
(字符串结尾 "hello" "")    ;; => 真
```
