---
title: 字符串行
---

# 类型

```meta-lisp
(-> 字符串型 (列表型 字符串型))
```

# 描述

按换行符将字符串拆分为多行。

# 例子

```meta-lisp
(字符串行 "a\nb\nc")  ;; => ["a" "b" "c"]
(字符串行 "abc")      ;; => ["abc"]
(字符串行 "")         ;; => [""]
```
