---
title: 字符串转大写
---

# 类型

```meta-lisp
(-> 字符串型 字符串型)
```

# 描述

将字符串中的所有字母转为大写。

# 例子

```meta-lisp
(字符串转大写 "hello")  ;; => "HELLO"
(字符串转大写 "Hello")  ;; => "HELLO"
(字符串转大写 "123")    ;; => "123"
```
