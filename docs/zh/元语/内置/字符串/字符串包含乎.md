---
title: 字符串包含乎
---

# 类型

```meta-lisp
(-> 字符串型 字符串型 布尔型)
```

# 描述

判断字符串是否包含指定子串。

# 例子

```meta-lisp
(字符串包含乎 "hello" "ell")  ;; => 真
(字符串包含乎 "hello" "xyz")  ;; => 假
(字符串包含乎 "hello" "")     ;; => 真
```
