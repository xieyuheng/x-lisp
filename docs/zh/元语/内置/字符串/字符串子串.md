---
title: 字符串子串
---

# 类型

```meta-lisp
(-> 整数型 整数型 字符串型 字符串型)
```

# 描述

取字符串的子串，从 `start`（含）到 `end`（不含）。索引以 Unicode 标量值计。

# 例子

```meta-lisp
(字符串子串 0 3 "hello")  ;; => "hel"
(字符串子串 1 4 "hello")  ;; => "ell"
(字符串子串 0 0 "hello")  ;; => ""
```
