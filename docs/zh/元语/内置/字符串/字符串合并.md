---
title: 字符串合并
---

# 类型

```meta-lisp
(-> 字符串型 (列表型 字符串型) 字符串型)
```

# 描述

用分隔符连接字符串列表，是 `字符串分割` 的逆操作。

# 例子

```meta-lisp
(字符串合并 "," ["a" "b" "c"])  ;; => "a,b,c"
(字符串合并 " " ["a" "b"])      ;; => "a b"
(字符串合并 "" ["a" "b"])       ;; => "ab"
```
