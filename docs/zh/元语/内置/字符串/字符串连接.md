---
title: 字符串连接
---

# 类型

```meta-lisp
(-> (列表型 字符串型) 字符串型)
```

# 描述

拼接一个字符串列表，等价于对列表中的字符串依次应用 `字符串拼接`。

# 例子

```meta-lisp
(字符串连接 ["a" "b" "c"])  ;; => "abc"
(字符串连接 [])             ;; => ""
(字符串连接 ["hello " "world"])  ;; => "hello world"
```
