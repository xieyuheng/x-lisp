---
title: 字符串包含
---

# 类型

```meta-lisp
(-> 字符串型 字符串型 真假型)
```

# 描述

判断字符串是否包含指定子串。

# 例子

```meta-lisp
(字符串包含 "hello" "ell")  ;; => 真
(字符串包含 "hello" "xyz")  ;; => 假
(字符串包含 "hello" "")     ;; => 真
```
