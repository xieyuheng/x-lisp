---
title: 字符串替换
---

# 类型

```meta-lisp
(-> 字符串型 字符串型 字符串型 字符串型)
```

# 描述

将字符串中所有出现的 `old` 替换为 `new`。

# 例子

```meta-lisp
(字符串替换 "hello world" "world" "there")  ;; => "hello there"
(字符串替换 "aaa" "a" "b")                  ;; => "bbb"
(字符串替换 "abc" "x" "y")                  ;; => "abc"
```
