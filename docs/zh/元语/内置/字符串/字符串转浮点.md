---
title: 字符串转浮点
---

# 类型

```meta-lisp
(-> 字符串型 浮点型)
```

# 描述

将字符串解析为浮点数。字符串不是合法浮点数格式时行为未定义。

# 例子

```meta-lisp
(字符串转浮点 "3.14")  ;; => 3.14
(字符串转浮点 "42")    ;; => 42.0
(字符串转浮点 "-1.5")  ;; => -1.5
```
