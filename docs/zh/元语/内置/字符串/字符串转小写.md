---
title: 字符串转小写
---

# 类型

```meta-lisp
(-> 字符串型 字符串型)
```

# 描述

将字符串中的所有字母转为小写。

# 例子

```meta-lisp
(字符串转小写 "HELLO")  ;; => "hello"
(字符串转小写 "Hello")  ;; => "hello"
(字符串转小写 "123")    ;; => "123"
```
