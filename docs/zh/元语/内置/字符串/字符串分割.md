---
title: 字符串分割
---

# 类型

```meta-lisp
(-> 字符串型 字符串型 (列表型 字符串型))
```

# 描述

按分隔符将字符串拆分为列表。

# 例子

```meta-lisp
(字符串分割 "a,b,c" ",")     ;; => ["a" "b" "c"]
(字符串分割 "hello" ",")     ;; => ["hello"]
(字符串分割 "" ",")          ;; => [""]
```
