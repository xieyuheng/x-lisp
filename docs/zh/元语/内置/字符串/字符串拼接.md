---
title: 字符串拼接
---

# 类型

```meta-lisp
(-> 字符串型 字符串型 字符串型)
```

# 描述

拼接两个字符串。

# 例子

```meta-lisp
(字符串拼接 "hello" " world")  ;; => "hello world"
(字符串拼接 "a" "b")           ;; => "ab"
(字符串拼接 "" "hello")        ;; => "hello"
```
