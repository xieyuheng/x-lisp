---
title: 字符串查找索引
---

# 类型

```meta-lisp
(-> 字符串型 字符串型 整数型)
```

# 描述

查找子串在字符串中第一次出现的位置。不存在时返回 `-1`。

# 例子

```meta-lisp
(字符串查找索引 "hello" "ll")  ;; => 2
(字符串查找索引 "hello" "x")   ;; => -1
(字符串查找索引 "hello" "")    ;; => 0
```
