---
title: 符号算式收集键值对
---

# 类型

```meta-lisp
(-> (列表型 符号算式型) (列表型 (序对型 标签型 符号算式型)))
```

# 描述

从带位置的 S 表达式列表中收集键值对。遇到 keyword 开头的 sexp 时，将 keyword 作为键、下一个 sexp 作为值。

# 例子

```meta-lisp
(符号算式收集键值对
  [标签符号算式 :key ... 整数符号算式 42 ...])
;; => [(作序对 :key 整数符号算式-42)]
```
