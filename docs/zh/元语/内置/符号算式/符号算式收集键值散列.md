---
title: 符号算式收集键值散列
---

# 类型

```meta-lisp
(-> (列表型 符号算式型) (散列型 关键字型 符号算式型))
```

# 描述

从带位置的 S 表达式列表中收集键值对为哈希表。与 `符号算式收集键值对` 类似，但返回哈希表。

# 例子

```meta-lisp
(符号算式收集键值散列
  [关键字符号算式 :key ... 整数符号算式 42 ...])
;; => @{:key 整数符号算式-42}
```
