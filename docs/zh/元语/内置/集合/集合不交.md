---
title: 集合不交
---

# 类型

```meta-lisp
(多态 (E) (-> (集合型 E) (集合型 E) 布尔型))
```

# 描述

判断两个集合是否不相交（没有公共元素）。

# 例子

```meta-lisp
(集合不交 (@集合 1 2) (@集合 3 4))  ;; => 真
(集合不交 (@集合 1 2) (@集合 2 3))  ;; => 假
```
