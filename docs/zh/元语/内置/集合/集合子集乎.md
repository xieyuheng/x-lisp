---
title: 集合子集乎
---

# 类型

```meta-lisp
(多态 (E) (-> (集合型 E) (集合型 E) 布尔型))
```

# 描述

判断第一个集合是否为第二个的子集。

# 例子

```meta-lisp
(集合子集乎 (@集合 1 2) (@集合 1 2 3))  ;; => 真
(集合子集乎 (@集合 1 2 3) (@集合 1 2))  ;; => 假
(集合子集乎 (@集合) (@集合 1 2 3))     ;; => 真
```
