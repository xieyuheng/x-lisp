---
title: 二元组型
---

# 类型

```meta-lisp
类型型
```

# 描述

二元组类型构造器。`(二元组型 A B)` 表示一个包含类型 `A` 和 `B` 两个值的对。

# 定义

```meta-lisp
(定义结构 (二元组型 A B)
  (第一 A)
  (第二 B))
```

# 自动生成

```meta-lisp
(声明 作二元组 (多态 (A B) (-> A B (二元组型 A B))))
(声明 为二元组 (多态 (A) (-> A 真假型)))
(声明 二元组第一  (多态 (A B) (-> (二元组型 A B) A)))
(声明 二元组第二 (多态 (A B) (-> (二元组型 A B) B)))
(声明 二元组置第一之  (多态 (A B) (-> A (二元组型 A B) (二元组型 A B))))
(声明 二元组置第二之 (多态 (A B) (-> B (二元组型 A B) (二元组型 A B))))
```

# 例子

```meta-lisp
(令 ((p (作二元组 1 "hello")))
  (二元组第一 p))   ;; => 1
```
