---
title: 二元组置第二之
---

# 类型

```meta-lisp
(多态 (A B) (-> B (二元组型 A B) (二元组型 A B)))
```

# 描述

替换 pair 的第二个元素。

# 例子

```meta-lisp
(令 ((p (作二元组 1 "hello")))
  (二元组置第二之 "world" p)
  (二元组第二 p))  ;; => "world"
```
