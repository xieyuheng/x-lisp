---
title: 格式化为符号算式
---

# 类型

```meta-lisp
(多态 (A) (-> A 字符串型))
```

# 描述

将任意值格式化为 S 表达式字符串。

# 例子

```meta-lisp
(格式化为符号算式 '(a b c))  ;; => "(a b c)"
(格式化为符号算式 42)        ;; => "42"
(格式化为符号算式 "hello")   ;; => "\"hello\""
```
