---
title: 整数升序比较
---

# 类型

```meta-lisp
(-> 整数型 整数型 整数型)
```

# 描述

升序比较。如果第一个小于第二个返回 `-1`，相等返回 `0`，大于返回 `1`。

# 例子

```meta-lisp
(整数升序比较 1 2)   ;; => -1
(整数升序比较 2 2)   ;; => 0
(整数升序比较 3 2)   ;; => 1
```
