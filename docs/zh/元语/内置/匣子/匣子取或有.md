---
title: 匣子取或有
---

# 类型

```meta-lisp
(多态 (甲) (-> (匣子型 甲) (或有型 甲)))
```

# 描述

取出 box 中的值。空 box 返回 `(无)`，有值返回 `(有 value)`。

# 例子

```meta-lisp
(定义 box (作匣子))
(匣子取或有 box) ;; => (无)
(匣子置之 42 box)
(匣子取或有 box) ;; => (有 42)
```
