---
title: 符号连接
---

# 类型

```meta-lisp
(-> (列表型 符号型) 符号型)
```

# 描述

拼接一个符号列表，生成一个新符号。

# 例子

```meta-lisp
(符号连接 ['foo 'bar 'baz])  ;; => 'foobarbaz
(符号连接 [])                ;; => ''
(符号连接 ['a 'b])           ;; => 'ab
```
