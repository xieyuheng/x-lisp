---
title: 三元组置第二之
---

# 类型

```meta-lisp
(多态 (A B C) (-> B (三元组型 A B C) (三元组型 A B C)))
```

# 描述

替换 triple 的第二个元素。

# 例子

```meta-lisp
(令 ((t (作三元组 1 "hello" #t)))
  (三元组置第二之 "world" t)
  (三元组第二 t))  ;; => "world"
```
