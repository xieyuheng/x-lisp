---
title: 三元组置第三之
---

# 类型

```meta-lisp
(多态 (A B C) (-> C (三元组型 A B C) (三元组型 A B C)))
```

# 描述

替换 triple 的第三个元素。

# 例子

```meta-lisp
(令 ((t (作三元组 1 "hello" #t)))
  (三元组置第三之 #f t)
  (三元组第三 t))  ;; => 假
```
