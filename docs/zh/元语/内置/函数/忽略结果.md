---
title: 忽略结果
---

# 类型

```meta-lisp
(多态 (A) (-> A 空型))
```

# 描述

接受任意值，返回 `空`。用于丢弃函数调用的返回值。

# 例子

```meta-lisp
(忽略结果 (散列置之 'x 1 (作散列)))  ;; => 空
(忽略结果 1)                             ;; => 空
```
