---
title: 遍历列表
---

# 类型

```meta-lisp
(多态 (A Any) (-> (列表型 A) (-> A Any) 空型))
```

# 描述

`遍历列表` 是 `列表逐项` 的 data-first 版本。
等价于 `(列表逐项 fn data)`。

对列表中每个元素执行副作用函数。

# 例子

```meta-lisp
(遍历列表 [1 2 3] 打印行)
```
