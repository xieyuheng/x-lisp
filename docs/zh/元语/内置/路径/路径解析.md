---
title: 路径解析
---

# 类型

```meta-lisp
(-> 文本型 文本型)
```

# 描述

将路径解析为绝对路径。如果 `path` 已经是绝对路径（以 `/` 开头），则直接返回；否则使用当前工作目录拼接并规范化。

# 例子

```meta-lisp
(路径解析 "/etc")   ;; => "/etc"
(路径解析 "foo")    ;; => "/home/user/foo"
(路径解析 "./bar")  ;; => "/home/user/bar"
```
