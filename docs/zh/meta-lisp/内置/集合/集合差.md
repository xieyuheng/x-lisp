---
title: 集合差
---

# 类型

```meta-lisp
(泛型 (甲) (-> (集合型 甲) (集合型 甲) (集合型 甲)))
```

# 描述

求两个集合的差集（第一个集合中有但第二个中没有的元素）。

# 例子

```meta-lisp
(集合差 (@集合 1 2 3) (@集合 2 3))  ;; => (@集合 1)
(集合差 (@集合 1 2) (@集合 1 2 3))  ;; => (@集合)
```
