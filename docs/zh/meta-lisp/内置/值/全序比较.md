---
title: 全序比较
---

# 类型

```meta-lisp
(泛型 (甲 乙) (-> 甲 乙 整数型))
```

# 描述

全序比较。返回负数、零或正数，分别表示第一个值小于、等于或大于第二个值。

# 例子

```meta-lisp
(全序比较 1 2)   ;; => 负数
(全序比较 2 2)   ;; => 0
(全序比较 3 2)   ;; => 正数
```
