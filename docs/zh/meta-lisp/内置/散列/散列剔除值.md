---
title: 散列剔除值
---

# 类型

```meta-lisp
(多态 (键 值) (-> (-> 值 真假型) (散列型 键 值) (散列型 键 值)))
```

# 描述

移除值满足谓词的条目。

# 例子

```meta-lisp
(散列剔除值
  整数为非负
  (@散列 'a 1 'b 2 'x -1 'y -2))
;; => (@散列 'x -1 'y -2)
```
