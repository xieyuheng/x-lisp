---
title: 散列映射值
---

# 类型

```meta-lisp
(泛型 (键 值甲 值乙) (-> (-> 值甲 值乙) (散列型 键 值甲) (散列型 键 值乙)))
```

# 描述

对值应用函数，键保持不变。

# 例子

```meta-lisp
(散列映射值 (函 (n) (整数加 10 n)) (@散列 'a 1 'b 2))
;; => (@散列 'a 11 'b 12)
```
