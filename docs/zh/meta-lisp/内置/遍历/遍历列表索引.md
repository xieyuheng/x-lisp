---
title: 遍历列表索引
---

# 类型

```meta-lisp
(泛型 (甲 任意) (-> (列表型 甲) (-> 整数型 甲 任意) 空型))
```

# 描述

`遍历列表索引` 是 `列表逐项索引` 的 data-first 版本。
等价于 `(列表逐项索引 fn data)`。

对列表中每个元素执行带索引的副作用函数。

# 例子

```meta-lisp
(遍历列表索引 ['a 'b 'c]
  (函 (i x)
    (打印行 i)
    (打印行 x)))
```
