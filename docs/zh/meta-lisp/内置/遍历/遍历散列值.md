---
title: 遍历散列值
---

# 类型

```meta-lisp
(多态 (键 值 任意) (-> (散列型 键 值) (-> 值 任意) 空型))
```

# 描述

`遍历散列值` 是 `散列逐值` 的 data-first 版本。
等价于 `(散列逐值 fn data)`。

遍历散列表中每个值并执行副作用。

# 例子

```meta-lisp
(遍历散列值 (@散列 1 2 3 4) 打印行)
```
