---
title: 遍历集合
---

# 类型

```meta-lisp
(多态 (甲 任意) (-> (集合型 甲) (-> 甲 任意) 空型))
```

# 描述

`遍历集合` 是 `集合逐项` 的 data-first 版本。
等价于 `(集合逐项 fn data)`。

对集合中每个元素执行副作用函数。

# 例子

```meta-lisp
(遍历集合 (@集合 1 2 3) 打印行)
```
