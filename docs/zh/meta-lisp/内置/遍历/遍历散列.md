---
title: 遍历散列
---

# 类型

```meta-lisp
(泛型 (键 值 任意) (-> (散列型 键 值) (-> 键 值 任意) 空型))
```

# 描述

`遍历散列` 是 `散列逐项` 的数据优先版本。
等价于 `(散列逐项 函数 数据)`。

遍历散列表中每个键值对并执行副作用。

# 例子

```meta-lisp
(遍历散列 (@散列 1 2 3 4)
  (函 (键 值)
    (打印行 键)
    (打印行 值)))
```
