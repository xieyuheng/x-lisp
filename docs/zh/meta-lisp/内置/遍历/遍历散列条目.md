---
title: 遍历散列条目
---

# 类型

```meta-lisp
(泛型 (键 值 任意) (-> (散列型 键 值) (-> (序对型 键 值) 任意) 空型))
```

# 描述

`遍历散列条目` 是 `散列逐条目` 的数据优先版本。
等价于 `(散列逐条目 函数 数据)`。

遍历散列表中每个条目并执行副作用。

# 例子

```meta-lisp
(遍历散列条目 (@散列 1 2 3 4) 打印行)
```
