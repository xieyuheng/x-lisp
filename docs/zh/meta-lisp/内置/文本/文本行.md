---
title: 文本行
---

# 类型

```meta-lisp
(-> 文本型 (列表型 文本型))
```

# 描述

按换行符将文本拆分为多行。

# 例子

```meta-lisp
(文本行 "a\nb\nc")  ;; => ["a" "b" "c"]
(文本行 "abc")      ;; => ["abc"]
(文本行 "")         ;; => [""]
```
