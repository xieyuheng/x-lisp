---
title: 匣子取或有
---

# 类型

```meta-lisp
(泛型 (甲) (-> (匣子型 甲) (或有型 甲)))
```

# 描述

取出匣子中的值。空匣子返回 `(无)`，有值返回 `(有 值)`。

# 例子

```meta-lisp
(定义 匣子 (作匣子))
(匣子取或有 匣子) ;; => (无)
(匣子存 42 匣子)
(匣子取或有 匣子) ;; => (有 42)
```
