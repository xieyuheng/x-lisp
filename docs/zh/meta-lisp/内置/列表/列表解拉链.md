---
title: 列表解拉链
---

# 类型

```meta-lisp
(泛型 (甲 乙) (-> (列表型 (序对型 甲 乙)) (序对型 (列表型 甲) (列表型 乙))))
```

# 描述

将序对列表拆分为两个列表。

# 例子

```meta-lisp
(列表解拉链 [(作序对 'a 1) (作序对 'b 2)])  ;; => (作序对 ['a 'b] [1 2])
```
