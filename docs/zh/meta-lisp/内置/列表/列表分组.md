---
title: 列表分组
---

# 类型

```meta-lisp
(泛型 (键 值) (-> (-> 值 键) (列表型 值) (散列型 键 (列表型 值))))
```

# 描述

按键函数对列表进行分组，返回散列表。

# 例子

```meta-lisp
;; (交换参数 整数模 3) 翻转参数：(交换参数 整数模 3) => (函 (数) (整数模 数 3))
(列表分组 (交换参数 整数模 3) [0 1 2 3 4 5])
;; => (@散列 0 [0 3] 1 [1 4] 2 [2 5])
```
