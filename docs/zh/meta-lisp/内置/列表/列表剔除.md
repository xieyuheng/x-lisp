---
title: 列表剔除
---

# 类型

```meta-lisp
(多态 (甲) (-> (-> 甲 真假型) (列表型 甲) (列表型 甲)))
```

# 描述

筛选列表中不满足条件的元素，去除匹配的。

# 例子

```meta-lisp
(列表剔除 为整数 ['a 1 'b 2])       ;; => ['a 'b]
(列表剔除 整数为非负 [0 1 -1 2])  ;; => [-1]
```
