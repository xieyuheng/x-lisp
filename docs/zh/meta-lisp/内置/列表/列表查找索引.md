---
title: 列表查找索引
---

# 类型

```meta-lisp
(多态 (甲) (-> (-> 甲 真假型) (列表型 甲) 整数型))
```

# 描述

查找列表中第一个满足条件的元素的索引。不存在时返回 `-1`。

# 例子

```meta-lisp
(列表查找索引 为整数 ['a 'b 3 'd])  ;; => 2
(列表查找索引 为整数 ['a 'b 'c])    ;; => -1
```
