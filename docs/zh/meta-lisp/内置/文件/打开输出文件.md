---
title: 打开输出文件
---

# 类型

```meta-lisp
(-> 文本型 文件型)
```

# 描述

以写入模式打开文件。接受文件路径，返回文件句柄。如果文件不存在则创建，存在则覆盖。

# 例子

```meta-lisp
(令 ((file (打开输出文件 "output.txt")))
  (文件写行 file "hello")
  (文件关闭 file))
```
