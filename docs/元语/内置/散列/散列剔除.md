---
title: 散列剔除
---

# 类型

```meta-lisp
(泛型 (键 值) (-> (-> 键 值 真假型) (散列型 键 值) (散列型 键 值)))
```

# 描述

移除谓词返回真的条目。

# 例子

```meta-lisp
(散列剔除
  (函 (键 值) (整数非负 值))
  (@散列 'a 1 'b 2 'x -1 'y -2))
;; => (@散列 'x -1 'y -2)
```
