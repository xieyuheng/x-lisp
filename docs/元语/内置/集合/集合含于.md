---
title: 集合含于
---

# 类型

```meta-lisp
(泛型 (甲) (-> (集合型 甲) (集合型 甲) 真假型))
```

# 描述

判断第一个集合是否含于第二个集合。

# 例子

```meta-lisp
(集合含于 (@集合 1 2) (@集合 1 2 3))  ;; => 真
(集合含于 (@集合 1 2 3) (@集合 1 2))  ;; => 假
(集合含于 (@集合) (@集合 1 2 3))     ;; => 真
```
