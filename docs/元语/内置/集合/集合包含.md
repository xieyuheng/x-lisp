---
title: 集合包含
---

# 类型

```meta-lisp
(泛型 (甲) (-> (集合型 甲) (集合型 甲) 真假型))
```

# 描述

判断第一个集合是否包含第二个集合。

# 例子

```meta-lisp
(集合包含 (@集合 1 2 3) (@集合 1 2))  ;; => 真
(集合包含 (@集合 1 2) (@集合 1 2 3))  ;; => 假
(集合包含 (@集合 1 2 3) (@集合))     ;; => 真
```
