---
title: 整数降序比较
---

# 类型

```meta-lisp
(-> 整数型 整数型 整数型)
```

# 描述

降序比较。如果第一个大于第二个返回 `-1`，相等返回 `0`，小于返回 `1`。

# 例子

```meta-lisp
(整数降序比较 3 2)  ;; => -1
(整数降序比较 2 2)  ;; => 0
(整数降序比较 1 2)  ;; => 1
```
