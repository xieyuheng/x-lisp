---
title: 丢弃参数
---

# 类型

```meta-lisp
(泛型 (甲 乙 丙)
  (-> (-> 甲 乙)
      (-> 丙 甲 乙)))
```

# 描述

创建一个忽略第一个参数的新函数。

# 例子

```meta-lisp
(定义 (问候 名) (打印行 (文本拼接 "Hello, " 名)))
((丢弃参数 问候) "ignored" "World")  ;; 输出 "Hello, World"
```
