---
title: 忽略结果
---

# 类型

```meta-lisp
(泛型 (甲) (-> 甲 空值型))
```

# 描述

接受任意值，返回 `空值`。用于丢弃函数调用的返回值。

# 例子

```meta-lisp
(忽略结果 (散列存 (作散列) 'x 1))  ;; => 空值
(忽略结果 1)                             ;; => 空值
```
