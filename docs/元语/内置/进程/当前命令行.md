---
title: 当前命令行
---

# 类型

```meta-lisp
(-> (列表型 文本型))
```

# 描述

获取 `--` 分隔符之后的命令行参数。

当 xvm 可执行文件以 `--` 调用时，`--` 之后的所有参数将被收集为当前命令行。
例如：

```
xvm run program.x86.exe -- check --verbose
```

`(当前命令行)` 返回 `["check" "--verbose"]`。

# 例子

```meta-lisp
(当前命令行)  ;; => ["check" "--verbose"]
```
