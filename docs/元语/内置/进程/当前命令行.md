---
title: 当前命令行
---

# 类型

```meta-lisp
(-> (列表型 文本型))
```

# 描述

获取 `--` 分隔符之后的命令行参数。

当 xvm2 可执行文件以 `--` 调用时，`--` 之后的所有参数将被收集为当前命令行。
例如：

```
xvm2 run program.x86.exe -- check --profile
```

`(当前命令行)` 返回 `["check" "--profile"]`。

# 例子

```meta-lisp
(当前命令行)  ;; => ["check" "--profile"]
```
