---
title: 相同
---

# 类型

```meta-lisp
(泛型 (甲 乙) (-> 甲 乙 真假型))
```

# 描述

判断两个值是否原子相等或引用相等。

# 例子

```meta-lisp
(相同 1 1)              ;; => 真
(相同 "a" "a")          ;; => 真
(相同 (@列表 1 2 3) (@列表 1 2 3))  ;; => 假
```
