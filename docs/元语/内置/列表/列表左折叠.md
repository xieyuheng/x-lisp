---
title: 列表左折叠
---

# 类型

```meta-lisp
(泛型 (甲 乙) (-> (列表型 甲) 乙 (-> 乙 甲 乙) 乙))
```

# 描述

从左到右折叠列表。

# 例子

```meta-lisp
(列表左折叠 (@列表 1 2 3 4) 0 整数加)          ;; => 10
(列表左折叠 (@列表 1 2 3 4) (@列表) (交换参数 添))  ;; => (@list 4 3 2 1)
```
