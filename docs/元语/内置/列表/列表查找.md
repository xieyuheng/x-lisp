---
title: 列表查找
---

# 类型

```meta-lisp
(泛型 (甲) (-> (-> 甲 真假型) (列表型 甲) (或有型 甲)))
```

# 描述

查找列表中第一个满足条件的元素，返回 `(有 值)` 或 `(无)`。

# 例子

```meta-lisp
(列表查找 为整数 (@列表 'a 'b 3 'd))  ;; => (有 3)
(列表查找 为整数 (@列表 'a 'b 'c))    ;; => (无)
```
