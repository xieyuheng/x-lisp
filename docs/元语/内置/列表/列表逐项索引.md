---
title: 列表逐项索引
---

# 类型

```meta-lisp
(泛型 (甲 任意) (-> (列表型 甲) (-> 整数型 甲 任意) 空值型))
```

# 描述

对列表中的每个元素执行带索引的副作用函数。

# 例子

```meta-lisp
(列表逐项索引
 (@列表 'a 'b 'c)
 (函 (索引 元素)
   (打印 索引)
   (打印 ": ")
   (打印行 元素)))
;; 输出：
;; 0: a
;; 1: b
;; 2: c
```
