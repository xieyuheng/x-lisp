---
title: 列表拉链
---

# 类型

```meta-lisp
(泛型 (甲 乙) (-> (列表型 甲) (列表型 乙) (列表型 (序对型 甲 乙))))
```

# 描述

将两个列表按对应位置配对。

# 例子

```meta-lisp
(列表拉链 (@列表 'a 'b 'c) (@列表 1 2 3))   ;; => (@list (作序对 'a 1) (作序对 'b 2) (作序对 'c 3))
```
