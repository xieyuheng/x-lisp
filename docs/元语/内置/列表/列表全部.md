---
title: 列表全部
---

# 类型

```meta-lisp
(泛型 (甲) (-> (-> 甲 真假型) (列表型 甲) 真假型))
```

# 描述

判断列表中是否所有元素都满足条件。空列表返回 `真`。

# 例子

```meta-lisp
(列表全部 整数非负 [0 1 2 3])  ;; => 真
(列表全部 整数非负 [0 1 -1])   ;; => 假
```
