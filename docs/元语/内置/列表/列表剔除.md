---
title: 列表剔除
---

# 类型

```meta-lisp
(泛型 (甲) (-> (-> 甲 真假型) (列表型 甲) (列表型 甲)))
```

# 描述

筛选列表中不满足条件的元素，去除匹配的。

# 例子

```meta-lisp
(列表剔除 为整数 (@列表 'a 1 'b 2))       ;; => (@list 'a 'b)
(列表剔除 整数非负 (@列表 0 1 -1 2))  ;; => (@list -1)
```
