---
title: 列表映射
---

# 类型

```meta-lisp
(泛型 (甲 乙) (-> (-> 甲 乙) (列表型 甲) (列表型 乙)))
```

# 描述

对列表中的每个元素应用函数，返回结果列表。

# 例子

```meta-lisp
(列表映射 (整数加 10) (@列表 1 2 3))  ;; => (@list 11 12 13)
(列表映射 为文本 (@列表 1 "a" 3))  ;; => (@list 假 真 假)
```
