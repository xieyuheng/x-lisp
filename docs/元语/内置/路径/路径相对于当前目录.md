---
title: 路径相对于当前目录
---

# 类型

```meta-lisp
(-> 文本型 文本型)
```

# 描述

返回 `路径` 相对于当前工作目录（cwd）的相对路径。

注意：cwd 始终是绝对路径，因此传入的 `路径` 必须是绝对路径。
若 `路径` 为相对路径，将触发断言错误。

# 例子

```meta-lisp
(路径相对于当前目录 "/home/user/package/src")  ;; 例如 => "src"
(路径相对于当前目录 "/home/user")               ;; 例如 => ".."
```
