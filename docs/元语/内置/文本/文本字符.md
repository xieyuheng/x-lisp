---
title: 文本字符
---

# 类型

```meta-lisp
(-> 文本型 (列表型 文本型))
```

# 描述

将文本拆分为单个字符的文本列表。

# 例子

```meta-lisp
(文本字符 "abc")   ;; => (@list "a" "b" "c")
(文本字符 "")      ;; => (@list)
(文本字符 "你好")  ;; => (@list "你" "好")
```
