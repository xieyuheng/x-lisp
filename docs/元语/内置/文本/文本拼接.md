---
title: 文本拼接
---

# 类型

```meta-lisp
(-> (列表型 文本型) 文本型)
```

# 描述

拼接一个文本列表，等价于对列表中的文本依次应用 `文本追加`。

# 例子

```meta-lisp
(文本拼接 ["a" "b" "c"])  ;; => "abc"
(文本拼接 [])             ;; => ""
(文本拼接 ["hello " "world"])  ;; => "hello world"
```
