---
title: 文本截取
---

# 类型

```meta-lisp
(-> 整数型 整数型 文本型 文本型)
```

# 描述

取文本的片段，从 `start`（含）到 `end`（不含）。索引以 Unicode 标量值计。

# 例子

```meta-lisp
(文本截取 0 3 "hello")  ;; => "hel"
(文本截取 1 4 "hello")  ;; => "ell"
(文本截取 0 0 "hello")  ;; => ""
```
