---
title: 文本替换
---

# 类型

```meta-lisp
(-> 文本型 文本型 文本型 文本型)
```

# 描述

将文本中所有出现的 `old` 替换为 `new`。

# 例子

```meta-lisp
(文本替换 "world" "there" "hello world")  ;; => "hello there"
(文本替换 "a" "b" "aaa")                  ;; => "bbb"
(文本替换 "x" "y" "abc")                  ;; => "abc"
```
