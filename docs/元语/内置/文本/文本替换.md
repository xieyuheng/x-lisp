---
title: 文本替换
---

# 类型

```meta-lisp
(-> 文本型 文本型 文本型 文本型)
```

# 描述

将文本中所有出现的 `old` 替换为 `new`。

# 例子

```meta-lisp
(文本替换 "hello world" "world" "there")  ;; => "hello there"
(文本替换 "aaa" "a" "b")                  ;; => "bbb"
(文本替换 "abc" "x" "y")                  ;; => "abc"
```
