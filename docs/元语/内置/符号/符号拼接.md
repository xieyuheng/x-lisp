---
title: 符号拼接
---

# 类型

```meta-lisp
(-> (列表型 符号型) 符号型)
```

# 描述

拼接一个符号列表，生成一个新符号。

# 例子

```meta-lisp
(符号拼接 (@列表 'foo 'bar 'baz))  ;; => 'foobarbaz
(符号拼接 (@列表))                ;; => ''
(符号拼接 (@列表 'a 'b))           ;; => 'ab
```
