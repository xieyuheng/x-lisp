---
title: 遍历列表索引
---

# 类型

```meta-lisp
(泛型 (甲 任意) (-> (列表型 甲) (-> 整数型 甲 任意) 空型))
```

# 描述

`遍历列表索引` 是 `列表逐项索引` 的数据优先版本。
等价于 `(列表逐项索引 函数 数据)`。

对列表中每个元素执行带索引的副作用函数。

# 例子

```meta-lisp
(遍历列表索引 (@列表 'a 'b 'c)
  (函 (索引 元素)
    (打印行 索引)
    (打印行 元素)))
```
