---
title: 浮点求和
---

# 类型

```meta-lisp
(-> (列表型 浮点型) 浮点型)
```

# 描述

求浮点数列表的和。空列表的和为 0.0。

# 例子

```meta-lisp
(浮点求和 (@列表 1.0 2.0 3.0))  ;; => 6.0
(浮点求和 (@列表))             ;; => 0.0
(浮点求和 (@列表 -1.0 0.0 1.0)) ;; => 0.0
```
