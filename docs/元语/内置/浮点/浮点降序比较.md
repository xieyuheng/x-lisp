---
title: 浮点降序比较
---

# 类型

```meta-lisp
(-> 浮点型 浮点型 整数型)
```

# 描述

降序比较。如果第一个大于第二个返回 `-1`，相等返回 `0`，小于返回 `1`。

# 例子

```meta-lisp
(浮点降序比较 3.0 2.0)  ;; => -1
(浮点降序比较 2.0 2.0)  ;; => 0
(浮点降序比较 1.0 2.0)  ;; => 1
```
