---
title: 浮点升序比较
---

# 类型

```meta-lisp
(-> 浮点型 浮点型 整数型)
```

# 描述

升序比较。如果第一个小于第二个返回 `-1`，相等返回 `0`，大于返回 `1`。

# 例子

```meta-lisp
(浮点升序比较 1.0 2.0)  ;; => -1
(浮点升序比较 2.0 2.0)  ;; => 0
(浮点升序比较 3.0 2.0)  ;; => 1
```
