# 使用方式

给 AI 的 prompt 格式：

```
先读以下文件，再实现：

1. prompts/migrate-passes/common.md
2. prompts/migrate-passes/040-execute-pass.md

先读完两个文件，然后按照第二份文件的指示实现。
```

替换文件名即可。每个 prompt 文件参考 `common.md` 中的通用指导，自身只包含任务特有内容。
