(module local-define-sequential-binding)

(define-test sequential-binding-test
  (define a 1)
  (define b (iadd a 1))
  (assert-equal 2 b))

(define-test non-head-local-define-test
  (let ((one 1))
    (define a one)
    (define b (iadd a one))
    (assert-equal 2 b)))
