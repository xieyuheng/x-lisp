(module example)

(claim identity
  (all (A)
    (-> A A)))

(define (identity x) x)

(define-test identity-test
  (let ((add1 (iadd 1)))
    (assert-equal 2 (add1 1)))
  (let ((add1 (identity (iadd (identity 1)))))
    (the (-> int-t int-t) identity)
    (the (-> float-t float-t) identity)
    (the (all (A) (-> A A)) identity)
    (assert-equal 2 (add1 1))))
