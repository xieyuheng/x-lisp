(module override-type-t)

(define-enum type-t
  (a-type)
  (b-type))

(define-test override-type-t-test
  (assert (is-a-type (a-type)))
  (assert (is-b-type (b-type)))
  (assert-not (is-a-type (b-type)))
  (assert-not (is-b-type (a-type))))
