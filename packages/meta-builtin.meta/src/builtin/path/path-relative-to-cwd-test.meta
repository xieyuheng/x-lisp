(module builtin)

(define-test path-relative-to-cwd-test
  (assert (path-is-relative (path-relative-to-cwd "/a/b")))
  (assert (not (path-is-absolute (path-relative-to-cwd "/a/b"))))
  (assert (is-text (path-relative-to-cwd "/tmp"))))
