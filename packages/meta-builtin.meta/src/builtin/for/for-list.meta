(module builtin)

(claim for-list
  (all (A Any)
    (-> (list-t A) (-> A Any)
        void-t)))

(define (for-list data fn)
  (list-each fn data))

(define-test for-list-test
  (for-list (@list 1 2 3) println))
