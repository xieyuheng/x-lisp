(module builtin)

(claim list-append
  (polymorphic (A)
    (-> (list-t A) (list-t A)
        (list-t A))))

(define (list-append list rest)
  (if (list-is-empty list)
    rest
    (cons (car list) (list-append (cdr list) rest))))

(define-test list-append-test
  (assert-equal
    (list-append [1 2 3] [4 5 6])
    [1 2 3 4 5 6]))
