(module builtin)

(define-test atom-test
  (assert (is-atom 1))
  (assert (is-atom 1.5))
  (assert (is-atom 'a))
  (assert (is-atom "a"))
  (assert-not (is-atom (@list 1 2 3)))
  (assert-not (is-atom (@set 1 2 3)))
  (assert-not (is-atom (@hash "x" 1 "y" 2))))
