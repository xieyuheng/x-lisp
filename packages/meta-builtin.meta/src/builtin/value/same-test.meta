(module builtin)

(define-test same-test
  (assert (same 'a 'a))
  (assert-not (same 'a 'b))
  (assert (same "a" "a"))
  (assert-not (same "a" "b"))
  (let ((list (@list 1 2 3)))
    (assert (same list list))
    (assert-not (same list (@list 1 2 3)))))
