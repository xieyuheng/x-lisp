(module builtin)

(claim set-union-many
  (all (E)
    (-> (list-t (set-t E))
        (set-t E))))

(define (set-union-many sets)
  (list-fold-left set-union (make-set) sets))

(define-test set-union-many-test
  (assert-equal
    (@set 1 2 3 4 5 6)
    (set-union-many
     (@list (@set 1 2)
            (@set 3 4)
            (@set 5 6)))))
