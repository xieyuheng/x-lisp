(module meta)

(claim parse-stmt
  (-> sexp-t
      (stmt-t exp-t)))

(define (parse-stmt sexp)
  (match sexp
    ((list-sexp elements location)
     (= tag (symbol-sexp-content (car elements)))
     (parse-special-form-stmt tag (cdr elements) location))))

(claim parse-special-form-stmt
  (-> symbol-t (list-t sexp-t) source-location-t
      (stmt-t exp-t)))

(define (parse-special-form-stmt tag sexps location)
  (cond ((equal? tag 'define)
         (cond ((list-sexp? (car sexps))
                (@comment (define (<name> <parameter> ...)
                            <body>))
                (define-function-stmt
                  (symbol-sexp-content (car (list-sexp-elements (car sexps))))
                  (list-map symbol-sexp-content (cdr (list-sexp-elements (car sexps))))
                  (parse-body (cdr sexps) location)
                  location))
               (else
                 (@comment (define <name> <exp>))
                 (define-variable-stmt
                   (symbol-sexp-content (car sexps))
                   (parse-body (cdr sexps) location)
                   location))))
        ((equal? tag 'define-test)
         (@comment (define-test <test-name> <body>))
         (define-test-stmt
           (symbol-sexp-content (car sexps))
           (parse-body (cdr sexps) location)
           location))
        ((equal? tag 'define-type)
         (cond ((list-sexp? (car sexps))
                (@comment (define-type (<type-name> <type-parameter> ...)
                            <body>))
                (define-type-stmt
                  (symbol-sexp-content (car (list-sexp-elements (car sexps))))
                  (list-map symbol-sexp-content (cdr (list-sexp-elements (car sexps))))
                  (parse-body (cdr sexps) location)
                  location))
               (else
                 (@comment (define-type <type-name>
                             <body>))
                 (define-type-stmt
                   (symbol-sexp-content (car sexps))
                   []
                   (parse-body (cdr sexps) location)
                   location))))
        ((equal? tag 'exempt)
         (@comment (exempt <name> ...))
         (exempt-stmt
           (list-map symbol-sexp-content sexps)
           location))
        ((equal? tag 'private)
         (@comment (private <name> ...))
         (private-stmt
           (list-map symbol-sexp-content sexps)
           location))
        ((equal? tag 'module)
         (@comment (module <module-name>))
         (declare-module-stmt
           (symbol-sexp-content (car sexps))
           location))
        ((equal? tag 'import)
         (@comment (import <module-name> <name> ...))
         (= source (parse-import-source (car sexps)))
         (import-stmt
           (pair-first source)
           (pair-second source)
           (list-map symbol-sexp-content (cdr sexps))
           location))
        ((equal? tag 'import-as)
         (@comment (import-as <module-name> <prefix>))
         (= source (parse-import-source (car sexps)))
         (import-as-stmt
           (pair-first source)
           (pair-second source)
           (symbol-sexp-content (car (cdr sexps)))
           location))
        ((equal? tag 'import-all)
         (@comment (import-all <module-name>))
         (= source (parse-import-source (car sexps)))
         (import-all-stmt
           (pair-first source)
           (pair-second source)
           location))
        ((equal? tag 'define-enum)
         (@comment (define-enum <type-name>
                     (<constructor-name> (<field-name> <type>) ...)
                     ...)
                   (define-enum (<type-name> <type-parameter> ...)
                     (<constructor-name> (<field-name> <type>) ...)
                     ...))
         (define-enum-stmt
           (parse-pre-type-constructor (car sexps))
           (list-map parse-pre-data-constructor (cdr sexps))
           location))
        ((equal? tag 'define-algebraic-type)
         (@comment (define-algebraic-type <type-name>
                     ((<constructor-name> (<field-name> <type>) ...)
                      <predicate-name>
                      (<field-name> <accessor-name> <modifier-name>)
                      ...)
                     ...)
                   (define-algebraic-type (<type-name> <type-parameter> ...)
                     ((<constructor-name> (<field-name> <type>) ...)
                      <predicate-name>
                      (<field-name> <accessor-name> <modifier-name>)
                      ...)
                     ...))
         (define-algebraic-type-stmt
           (parse-pre-type-constructor (car sexps))
           (list-map (lambda (sexp)
                       (match sexp
                         ((list-sexp elements location)
                          (parse-explicit-data-constructor elements location))))
                     (cdr sexps))
           location))
        ((equal? tag 'define-struct)
         (@comment (define-struct <type-name>
                     (<field-name> <type>)
                     ...)
                   (define-struct (<type-name> <type-parameter> ...)
                     (<field-name> <type>)
                     ...))
         (define-struct-stmt
           (parse-pre-type-constructor (car sexps))
           (list-map parse-pre-data-field (cdr sexps))
           location))
        ((equal? tag 'define-struct*)
         (@comment (define-struct* <type-name>
                     (<constructor-name>
                       (<field-name> <type>)
                       ...))
                   (define-struct* (<type-name> <type-parameter> ...)
                     (<constructor-name>
                       (<field-name> <type>)
                       ...)))
         (define-struct-star-stmt
           (parse-pre-type-constructor (car sexps))
           (parse-pre-data-constructor (car (cdr sexps)))
           location))
        ((equal? tag 'define-record-type)
         (@comment (define-record-type <type-name>
                     (<constructor-name> (<field-name> <type>) ...)
                     <predicate-name>
                     (<field-name> <accessor-name> <modifier-name>)
                     ...)
                   (define-record-type (<type-name> <type-parameter> ...)
                     (<constructor-name> (<field-name> <type>) ...)
                     <predicate-name>
                     (<field-name> <accessor-name> <modifier-name>)
                     ...))
         (define-record-type-stmt
           (parse-pre-type-constructor (car sexps))
           (parse-explicit-data-constructor (cdr sexps) location)
           location))
        ((equal? tag 'define-opaque-type)
         (@comment (define-opaque-type <type-name> <representation-type>
                     (<interface-name> <interface-type>)
                     ...)
                   (define-opaque-type (<type-name> <type-parameter> ...) <representation-type>
                     (<interface-name> <interface-type>)
                     ...))
         (= type-constructor (parse-pre-type-constructor (car sexps)))
         (define-opaque-type-stmt
           type-constructor
           (parse-exp (car (cdr sexps)))
           (list-map parse-pre-interface-entry (cdr (cdr sexps)))
           location))
        ((equal? tag 'claim)
         (@comment (claim <name> <type>))
         (claim-stmt
           (symbol-sexp-content (car sexps))
           (parse-exp (car (cdr sexps)))
           location))
        ((equal? tag 'claim-type)
         (@comment (claim-type <type-name>))
         (claim-type-stmt
           (symbol-sexp-content (car sexps))
           location))
        ((equal? tag 'admit)
         (@comment (admit <name> <type>))
         (admit-stmt
           (symbol-sexp-content (car sexps))
           (parse-exp (car (cdr sexps)))
           location))
        ((equal? tag 'declare-primitive-function)
         (@comment (declare-primitive-function <name> <arity>))
         (declare-primitive-function-stmt
           (symbol-sexp-content (car sexps))
           (int-sexp-content (car (cdr sexps)))
           location))
        ((equal? tag 'declare-primitive-variable)
         (@comment (declare-primitive-variable <name>))
         (declare-primitive-variable-stmt
           (symbol-sexp-content (car sexps))
           location))
        ((equal? tag '@comment)
         (@comment (@comment <sexp> ...))
         (comment-stmt sexps location))))

(claim parse-pre-type-constructor
  (-> sexp-t
      pre-type-constructor-t))

(define (parse-pre-type-constructor sexp)
  (match sexp
    ((list-sexp sexps location)
     (make-pre-type-constructor
       (symbol-sexp-content (car sexps))
       (list-map symbol-sexp-content (cdr sexps))))
    ((symbol-sexp content location)
     (make-pre-type-constructor content []))))

(claim parse-pre-data-constructor
  (-> sexp-t
      pre-data-constructor-t))

(define (parse-pre-data-constructor sexp)
  (match sexp
    ((list-sexp sexps location)
     (make-pre-data-constructor
       (symbol-sexp-content (car sexps))
       (list-map parse-pre-data-field (cdr sexps))
       location))))

(claim parse-pre-data-field
  (-> sexp-t
      pre-data-field-t))

(define (parse-pre-data-field sexp)
  (match sexp
    ((list-sexp sexps location)
     (make-pre-data-field
       (symbol-sexp-content (car sexps))
       (parse-exp (car (cdr sexps)))
       location))))

(define (parse-pre-interface-entry sexp)
  (match sexp
    ((list-sexp elements location)
     (make-pre-interface-entry
       (symbol-sexp-content (car elements))
       (parse-exp (car (cdr elements)))
       location))))

(claim parse-import-source
  (-> sexp-t
      (pair-t symbol-t symbol-t)))

(define (parse-import-source sexp)
  (= raw (symbol-sexp-content sexp))
  (= raw-string (symbol-to-string raw))
  (= parts (string-split "/" raw-string))
  (if (equal? (list-length parts) 1)
    (make-pair 'self raw)
    (make-pair (string-to-symbol (list-get 0 parts))
               (string-to-symbol (list-get 1 parts)))))

(claim parse-explicit-data-constructor
  (-> (list-t sexp-t) source-location-t
      (explicit-data-constructor-t exp-t)))

(define (parse-explicit-data-constructor sexps location)
  (@comment ((<constructor-name> (<field-name> <type>) ...)
             <predicate-name>
             (<field-name> <accessor-name> <modifier-name>)
             ...))
  (= data-constructor (parse-pre-data-constructor (car sexps)))
  (= group (list-get 0 sexps))
  (= accessor-list (list-drop 2 sexps))
  (= group-list (list-sexp-elements group))
  (= accessor-map (build-accessor-map accessor-list location))
  (= fields-raw (cdr group-list))
  (= fields
    (pipe fields-raw
      (list-map
        (lambda (field)
          (= field-list (list-sexp-elements field))
          (= name (symbol-sexp-content (car field-list)))
          (= type (parse-exp (car (cdr field-list))))
          (make-explicit-data-field
            name type
            (if (hash-has? name accessor-map)
              (pair-first (hash-get name accessor-map))
              (string-to-symbol
                (@string (symbol-to-string name) "-" (symbol-to-string name))))
            (if (hash-has? name accessor-map)
              (pair-second (hash-get name accessor-map))
              (nothing))
            location)))))
  (make-explicit-data-constructor
    (pre-data-constructor-name data-constructor)
    fields
    (symbol-sexp-content (car (cdr sexps)))
    location))

(claim build-accessor-map
  (-> (list-t sexp-t) source-location-t
      (hash-t symbol-t (pair-t symbol-t (maybe-t symbol-t)))))

(define (build-accessor-map accessors location)
  (if (list-empty? accessors)
    (make-hash)
    (begin
      (= entry (list-sexp-elements (car accessors)))
      (= rest (cdr accessors))
      (= field-name (symbol-sexp-content (car entry)))
      (= accessor-name (symbol-sexp-content (car (cdr entry))))
      (= modifier-name (if (int-greater? (list-length entry) 2)
                         (just (symbol-sexp-content (car (list-drop 2 entry))))
                         (nothing)))
      (hash-put field-name
                (make-pair accessor-name modifier-name)
                (build-accessor-map rest location)))))
