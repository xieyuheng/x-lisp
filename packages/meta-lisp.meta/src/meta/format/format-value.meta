(module meta)

(claim format-value (-> value-t string-t))
(define (format-value value)
  (match value
    ((type-value type)
     (format-type type))
    ((curry-value target arity args)
     (let ((args-str (format-values args))
           (target-str (format-value target)))
       (if (equal? args-str "")
         target-str
         (@string "(" target-str " " args-str ")"))))
    ((definition-value definition)
     (let ((mod-name (symbol-to-string (definition-mod-name definition)))
           (name (symbol-to-string (definition-name definition))))
       (@string mod-name "/" name)))))

(define (format-values values)
  (string-join " " (list-map format-value values)))
