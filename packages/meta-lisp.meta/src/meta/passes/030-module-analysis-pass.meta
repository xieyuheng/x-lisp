(module meta)

(define-struct module-analysis-report-t
  (defined-names name-group-by-mod-t)
  (private-names name-group-by-mod-t)
  (fragment-scopes (hash-t string-t fragment-scope-t))
  (outcome outcome-t))

;; - why: type aliases help us to understand the key type of hash-t.

(define-type mod-name-t symbol-t)
(define-type name-t symbol-t)
(define-type prefix-t symbol-t)

(define-type name-group-by-mod-t (hash-t mod-name-t (set-t name-t)))

(define-struct* fragment-scope-t
  (cons-fragment-scope
    (mod-name mod-name-t)
    (imported-names (hash-t name-t qualified-name-entry-t))
    (imported-prefixes (hash-t prefix-t prefix-entry-t))))

(define (make-fragment-scope mod-name)
  (cons-fragment-scope mod-name (make-hash) (make-hash)))

(define-struct qualified-name-entry-t
  (pkg-name symbol-t)
  (mod-name symbol-t)
  (name symbol-t))

(define-struct prefix-entry-t
  (pkg-name symbol-t)
  (mod-name symbol-t))

(claim module-analysis-pass
  (-> package-t
      module-analysis-report-t))

(define (module-analysis-pass package)
  (= report (make-module-analysis-report (make-hash) (make-hash) (make-hash) (outcome-ok)))
  (for-list (package-closure-in-topological-order package)
    (module-analysis-package report))
  (when (hash-has? "dump" (package-config-compiler (package-config package)))
    (dump-module-analysis-report report package))
  report)

(claim module-analysis-package
  (-> module-analysis-report-t package-t
      void-t))

(define (module-analysis-package report package)
  (for-list (package-closure-in-topological-order package)
    (lambda (ordered-package)
      (merge-set-hash!
        (module-analysis-report-defined-names report)
        (collect-defined-names ordered-package))
      (merge-set-hash!
        (module-analysis-report-private-names report)
        (collect-private-names ordered-package))
      (for-hash (package-fragments ordered-package)
        (lambda (path fragment)
          (= scope (make-fragment-scope (fragment-mod-name fragment)))
          (hash-put! path scope (module-analysis-report-fragment-scopes report))
          (for-list (fragment-stmts fragment)
            (lambda (stmt)
              (if (outcome-error? (ensure-imported-mod-exists ordered-package stmt))
                (ignore (module-analysis-report-put-outcome! (outcome-error) report))
                (execute-import ordered-package scope stmt)))))))))

(claim collect-defined-names
  (-> package-t
      name-group-by-mod-t))

(define (collect-defined-names package)
  (collect-names-by-mod
    package
    (compose set-to-list fragment-names)))

(claim collect-private-names
  (-> package-t
      name-group-by-mod-t))

(define (collect-private-names package)
  (collect-names-by-mod
    package
    (chain
      fragment-stmts
      (list-flat-map
        (lambda (stmt)
          (if (private-stmt? stmt)
            (private-stmt-names stmt)
            []))))))

(claim merge-set-hash!
  (polymorphic (K V)
    (-> (hash-t K (set-t V))
        (hash-t K (set-t V))
        void-t)))

(define (merge-set-hash! target source)
  (for-hash source
    (lambda (key values)
      (if (hash-has? key target)
        (let ((existing (hash-get key target)))
          (for-set values (lambda (v) (set-add! v existing))))
        (ignore (hash-put! key values target))))))

(claim collect-names-by-mod
  (-> package-t
      (-> fragment-t (list-t name-t))
      name-group-by-mod-t))

(define (collect-names-by-mod package extract)
  (= result (make-hash))
  (for-hash-value (package-fragments package)
    (lambda (fragment)
      (= mod-name (fragment-mod-name fragment))
      (if (hash-has? mod-name result)
        (let ((existing (hash-get mod-name result)))
          (for-list (extract fragment) (lambda (name) (set-add! name existing))))
        (ignore (hash-put! mod-name (list-to-set (extract fragment)) result)))))
  result)

(claim lookup-package
  (-> package-t symbol-t
      package-t))

(define (lookup-package package pkg-name)
  (if (equal? pkg-name 'self)
    package
    (hash-get pkg-name (package-dependencies package))))

(claim lookup-mod-defined-names
  (-> package-t symbol-t symbol-t
      (set-t name-t)))

(define (lookup-mod-defined-names package pkg-name mod-name)
  (= target (lookup-package package pkg-name))
  (= defined (collect-defined-names target))
  (if (hash-has? mod-name defined)
    (hash-get mod-name defined)
    (make-set)))

(claim lookup-mod-private-names
  (-> package-t symbol-t symbol-t
      (set-t name-t)))

(define (lookup-mod-private-names package pkg-name mod-name)
  (= target (lookup-package package pkg-name))
  (= private (collect-private-names target))
  (if (hash-has? mod-name private)
    (hash-get mod-name private)
    (make-set)))

(claim lookup-mod-public-names
  (-> package-t symbol-t symbol-t
      (set-t name-t)))

(define (lookup-mod-public-names package pkg-name mod-name)
  (set-difference
    (lookup-mod-defined-names package pkg-name mod-name)
    (lookup-mod-private-names package pkg-name mod-name)))

(claim ensure-imported-mod-exists
  (-> package-t (stmt-t exp-t)
      outcome-t))

(define (ensure-imported-mod-exists package stmt)
  (match stmt
    ((import-stmt pkg-name mod-name _ location)
     (ensure-mod-exists package pkg-name mod-name location))
    ((import-as-stmt pkg-name mod-name _ location)
     (ensure-mod-exists package pkg-name mod-name location))
    ((import-all-stmt pkg-name mod-name location)
     (ensure-mod-exists package pkg-name mod-name location))
    (else (outcome-ok))))

(claim execute-import
  (-> package-t fragment-scope-t (stmt-t exp-t)
      void-t))

(define (execute-import package scope stmt)
  (match stmt
    ((import-stmt pkg-name mod-name names location)
     (= imported-private-names (lookup-mod-private-names package pkg-name mod-name))
     (for-list names
       (lambda (name)
         (unless (set-member? name imported-private-names)
           (hash-put! name
             (make-qualified-name-entry pkg-name mod-name name)
             (fragment-scope-imported-names scope))))))
    ((import-as-stmt pkg-name mod-name prefix location)
     (hash-put! prefix
       (make-prefix-entry pkg-name mod-name)
       (fragment-scope-imported-prefixes scope))
     void)
    ((import-all-stmt pkg-name mod-name location)
     (= imported-public-names (lookup-mod-public-names package pkg-name mod-name))
     (= current-defined-names (lookup-mod-defined-names package 'self (fragment-scope-mod-name scope)))
     (for-set imported-public-names
       (lambda (name)
         ;; - why: skip names already defined in the current module,
         ;;   so that local definitions can override imported ones.
         (unless (set-member? name current-defined-names)
           (hash-put! name
             (make-qualified-name-entry pkg-name mod-name name)
             (fragment-scope-imported-names scope))))))
    (else void)))

(claim ensure-mod-exists
  (-> package-t symbol-t symbol-t source-location-t
      outcome-t))

(define (ensure-mod-exists package pkg-name mod-name location)
  (if (equal? pkg-name 'self)
    (if (not (has-fragment-of-mod-name? (hash-values (package-fragments package)) mod-name))
      (let ((message (@string "undefined module: " (symbol-to-string mod-name))))
        (writeln (format-message-with-source-location message location))
        (outcome-error))
      (outcome-ok))
    (if (not (hash-has? pkg-name (package-dependencies package)))
      (let ((message (@string "undefined package: " (symbol-to-string pkg-name))))
        (writeln (format-message-with-source-location message location))
        (outcome-error))
      (let ((dependency (hash-get pkg-name (package-dependencies package))))
        (if (not (has-fragment-of-mod-name? (hash-values (package-fragments dependency)) mod-name))
          (let ((message (@string "undefined module: " (symbol-to-string pkg-name) "/" (symbol-to-string mod-name))))
            (writeln (format-message-with-source-location message location))
            (outcome-error))
          (outcome-ok))))))

(claim has-fragment-of-mod-name?
  (-> (list-t fragment-t) symbol-t
      bool-t))

(define (has-fragment-of-mod-name? fragments mod-name)
  (list-some?
    (lambda (fragment)
      (equal? (fragment-mod-name fragment) mod-name))
    fragments))

(define (close-top lines)
  (= i (isub (list-length lines) 1))
  (list-put! i (@string (list-get i lines) ")") lines))

(define (sort-entries-by-symbol-key entries)
  (list-sort
    (lambda (a b)
      (string-compare-lexical
        (symbol-to-string (hash-entry-key a))
        (symbol-to-string (hash-entry-key b))))
    (hash-entries entries)))

(define (sort-entries-by-string-key entries)
  (list-sort
    (lambda (a b)
      (string-compare-lexical (hash-entry-key a) (hash-entry-key b)))
    (hash-entries entries)))

(define (format-name-value entry debug?)
  (@string
    (symbol-to-string (hash-entry-key entry)) " "
    (if debug?
      (let ((names (hash-entry-value entry)))
        (string-join " "
          (list-map symbol-to-string
            (list-sort
              (lambda (a b)
                (string-compare-lexical (symbol-to-string a) (symbol-to-string b)))
              (set-to-list names)))))
      (format (set-size (hash-entry-value entry))))))

(claim format-module-analysis-report
  (-> module-analysis-report-t bool-t
      string-t))

(define (format-module-analysis-report result debug?)
  (= lines [])
  (list-push! "(module-analysis-report" lines)

  (list-push! "  (defined-names" lines)
  (for-list (sort-entries-by-symbol-key (module-analysis-report-defined-names result))
    (lambda (entry)
      (list-push!
        (@string "    (" (format-name-value entry debug?) ")")
        lines)))
  (close-top lines)

  (list-push! "  (private-names" lines)
  (for-list (sort-entries-by-symbol-key (module-analysis-report-private-names result))
    (lambda (entry)
      (list-push!
        (@string "    (" (format-name-value entry debug?) ")")
        lines)))
  (close-top lines)

  (list-push! "  (fragment-scopes" lines)
  (for-list (sort-entries-by-string-key (module-analysis-report-fragment-scopes result))
    (lambda (entry)
      (= path (hash-entry-key entry))
      (= scope (hash-entry-value entry))
      (list-push! (@string "    (\"" path "\"") lines)
      (list-push!
        (@string "      (mod-name " (symbol-to-string (fragment-scope-mod-name scope)) ")")
        lines)
      (if debug?
        (begin
          (list-push! "      (imported-names" lines)
          (for-list (sort-entries-by-symbol-key (fragment-scope-imported-names scope))
            (lambda (entry)
              (= name (hash-entry-key entry))
              (= qualified-entry (hash-entry-value entry))
              (list-push!
                (@string "        (" (symbol-to-string name) " "
                  (symbol-to-string (qualified-name-entry-pkg-name qualified-entry)) "/"
                  (symbol-to-string (qualified-name-entry-mod-name qualified-entry)) "/"
                  (symbol-to-string (qualified-name-entry-name qualified-entry)) ")")
                lines)))
          (close-top lines)
          (list-push! "      (imported-prefixes" lines)
          (for-list (sort-entries-by-symbol-key (fragment-scope-imported-prefixes scope))
            (lambda (entry)
              (= prefix (hash-entry-key entry))
              (= prefix-entry (hash-entry-value entry))
              (list-push!
                (@string "        (" (symbol-to-string prefix) " "
                  (symbol-to-string (prefix-entry-pkg-name prefix-entry)) "/"
                  (symbol-to-string (prefix-entry-mod-name prefix-entry)) ")")
                lines)))
          (close-top lines))
        (begin
          (list-push!
            (@string "      (imported-names " (format (hash-length (fragment-scope-imported-names scope))) ")")
            lines)
          (list-push!
            (@string "      (imported-prefixes " (format (hash-length (fragment-scope-imported-prefixes scope))) ")")
            lines)))
      (close-top lines)))
  (close-top lines)

  (close-top lines)
  (string-join "\n" lines))

(claim dump-module-analysis-report
  (-> module-analysis-report-t package-t
      void-t))

(define (dump-module-analysis-report result package)
  (= dump-directory (path-join (package-output-directory package) "dump"))
  (fs-ensure-directory dump-directory)
  (= dump-path (@string dump-directory "/030-module-analysis-report.dump"))
  (fs-ensure-file dump-path)
  (fs-write dump-path (@string (format-module-analysis-report result false) "\n")))
