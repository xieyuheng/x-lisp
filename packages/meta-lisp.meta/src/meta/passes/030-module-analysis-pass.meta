(module meta)

(define-struct module-analysis-result-t
  (defined-names name-group-by-mod-t)
  (private-names name-group-by-mod-t)
  (fragment-scopes (hash-t string-t fragment-scope-t))
  (outcome outcome-t))

;; - why: type aliases help us to understand the key type of hash-t.

(define-type mod-name-t symbol-t)
(define-type name-t symbol-t)
(define-type prefix-t symbol-t)

(define-type name-group-by-mod-t (hash-t mod-name-t (set-t name-t)))

(define-struct* fragment-scope-t
  (cons-fragment-scope
    (mod-name mod-name-t)
    (imported-names (hash-t name-t qualified-name-entry-t))
    (imported-prefixes (hash-t prefix-t prefix-entry-t))))

(define (make-fragment-scope mod-name)
  (cons-fragment-scope mod-name (make-hash) (make-hash)))

(define-struct qualified-name-entry-t
  (pkg-name symbol-t)
  (mod-name symbol-t)
  (name symbol-t))

(define-struct prefix-entry-t
  (pkg-name symbol-t)
  (mod-name symbol-t))

(claim module-analysis-pass
  (-> package-t
      module-analysis-result-t))

(define (module-analysis-pass package)
  (= defined-names (make-hash))
  (= private-names (make-hash))
  (for-list (package-closure-in-topological-order package)
    (lambda (ordered-package)
      (merge-set-hash! defined-names (collect-defined-names package))
      (merge-set-hash! private-names (collect-private-names package))))
  (= fragment-scopes (make-hash))
  (= analysis-result
    (make-module-analysis-result defined-names private-names fragment-scopes (outcome-ok)))
  (for-list (package-closure-in-topological-order package)
    (lambda (ordered-package)
      (for-hash (package-fragments ordered-package)
        (lambda (path fragment)
          (= scope (make-fragment-scope (fragment-mod-name fragment)))
          (hash-put! path scope fragment-scopes)
          (for-list (fragment-stmts fragment)
            (lambda (stmt)
              (when (outcome-error? (execute-import ordered-package scope stmt))
                (module-analysis-result-put-outcome! (outcome-error) analysis-result))))))))
  analysis-result)

(claim collect-defined-names
  (-> package-t
      name-group-by-mod-t))

(define (collect-defined-names package)
  (collect-names-by-mod
    package
    (compose set-to-list fragment-names)))

(claim collect-private-names
  (-> package-t
      name-group-by-mod-t))

(define (collect-private-names package)
  (collect-names-by-mod
    package
    (lambda (fragment)
      (list-flat-map
        (lambda (stmt)
          (match stmt
            ((private-stmt names location) names)
            (else [])))
        (fragment-stmts fragment)))))

(claim merge-set-hash!
  (polymorphic (K V)
    (-> (hash-t K (set-t V))
        (hash-t K (set-t V))
        void-t)))

(define (merge-set-hash! target source)
  (for-hash source
    (lambda (key values)
      (if (hash-has? key target)
        (let ((existing (hash-get key target)))
          (for-set values (lambda (v) (set-add! v existing))))
        (ignore (hash-put! key values target))))))

(claim collect-names-by-mod
  (-> package-t
      (-> fragment-t (list-t name-t))
      name-group-by-mod-t))

(define (collect-names-by-mod package extract)
  (= result (make-hash))
  (for-hash-value (package-fragments package)
    (lambda (fragment)
      (= mod-name (fragment-mod-name fragment))
      (if (hash-has? mod-name result)
        (let ((existing (hash-get mod-name result)))
          (for-list (extract fragment) (lambda (name) (set-add! name existing))))
        (ignore (hash-put! mod-name (list-to-set (extract fragment)) result)))))
  result)

(claim lookup-package
  (-> package-t symbol-t
      package-t))

(define (lookup-package package pkg-name)
  (if (equal? pkg-name 'self)
    package
    (hash-get pkg-name (package-dependencies package))))

(claim lookup-mod-defined-names
  (-> package-t symbol-t symbol-t
      (set-t name-t)))

(define (lookup-mod-defined-names package pkg-name mod-name)
  (= target (lookup-package package pkg-name))
  (= defined (collect-defined-names target))
  (if (hash-has? mod-name defined)
    (hash-get mod-name defined)
    (make-set)))

(claim lookup-mod-private-names
  (-> package-t symbol-t symbol-t
      (set-t name-t)))

(define (lookup-mod-private-names package pkg-name mod-name)
  (= target (lookup-package package pkg-name))
  (= private (collect-private-names target))
  (if (hash-has? mod-name private)
    (hash-get mod-name private)
    (make-set)))

(claim lookup-mod-public-names
  (-> package-t symbol-t symbol-t
      (set-t name-t)))

(define (lookup-mod-public-names package pkg-name mod-name)
  (set-difference
    (lookup-mod-defined-names package pkg-name mod-name)
    (lookup-mod-private-names package pkg-name mod-name)))

(claim execute-import
  (-> package-t fragment-scope-t (stmt-t exp-t)
      outcome-t))

(define (execute-import package scope stmt)
  (match stmt
    ((import-stmt pkg-name mod-name names location)
     (if (outcome-error? (ensure-mod-exists package pkg-name mod-name location))
       (outcome-error)
       (begin
         (= imported-private-names (lookup-mod-private-names package pkg-name mod-name))
         (for-list names
           (lambda (name)
             (unless (set-member? name imported-private-names)
               (hash-put!
                 name
                 (make-qualified-name-entry pkg-name mod-name name)
                 (fragment-scope-imported-names scope)))))
         (outcome-ok))))
    ((import-as-stmt pkg-name mod-name prefix location)
     (if (outcome-error? (ensure-mod-exists package pkg-name mod-name location))
       (outcome-error)
       (begin
         (hash-put!
           prefix
           (make-prefix-entry pkg-name mod-name)
           (fragment-scope-imported-prefixes scope))
         (outcome-ok))))
    ((import-all-stmt pkg-name mod-name location)
     (if (outcome-error? (ensure-mod-exists package pkg-name mod-name location))
       (outcome-error)
       (begin
         (= imported-public-names (lookup-mod-public-names package pkg-name mod-name))
         (= current-defined-names (lookup-mod-defined-names package 'self (fragment-scope-mod-name scope)))
         (for-set imported-public-names
           (lambda (name)
             ;; - why: skip names already defined in the current module,
             ;;   so that local definitions can override imported ones.
             (unless (set-member? name current-defined-names)
               (hash-put!
                 name
                 (make-qualified-name-entry pkg-name mod-name name)
                 (fragment-scope-imported-names scope)))))
         (outcome-ok))))
    (else (outcome-ok))))

(claim ensure-mod-exists
  (-> package-t symbol-t symbol-t source-location-t
      outcome-t))

(define (ensure-mod-exists package pkg-name mod-name location)
  (if (equal? pkg-name 'self)
    (if (not (has-fragment-of-mod-name? (hash-values (package-fragments package)) mod-name))
      (let ((message (@string "undefined module: " (symbol-to-string mod-name))))
        (writeln (format-message-with-source-location message location))
        (outcome-error))
      (outcome-ok))
    (if (not (hash-has? pkg-name (package-dependencies package)))
      (let ((message (@string "undefined package: " (symbol-to-string pkg-name))))
        (writeln (format-message-with-source-location message location))
        (outcome-error))
      (let ((dependency (hash-get pkg-name (package-dependencies package))))
        (if (not (has-fragment-of-mod-name? (hash-values (package-fragments dependency)) mod-name))
          (let ((message (@string "undefined module: " (symbol-to-string pkg-name) "/" (symbol-to-string mod-name))))
            (writeln (format-message-with-source-location message location))
            (outcome-error))
          (outcome-ok))))))

(claim has-fragment-of-mod-name?
  (-> (list-t fragment-t) symbol-t
      bool-t))

(define (has-fragment-of-mod-name? fragments mod-name)
  (list-some?
    (lambda (fragment)
      (equal? (fragment-mod-name fragment) mod-name))
    fragments))
