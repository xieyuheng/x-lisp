(module meta)

(define (package-load-fragments package directory)
  (pipe (path-list-recursive directory)
    (list-each
      (lambda (name)
        (when (text-ends-with ".meta" name)
          (let ((path (path-join directory name))
                 (fragment (load-fragment path)))
            (package-put-fragment path fragment package)))))))
