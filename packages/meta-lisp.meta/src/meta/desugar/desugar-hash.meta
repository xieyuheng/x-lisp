(module meta)

(claim desugar-hash
  (-> (list-t (pair-t exp-t exp-t)) source-location-t
      exp-t))

(define (desugar-hash entries location)
  (define (entry-used-names entry)
    (set-union
      (exp-occurred-names (pair-first entry))
      (exp-occurred-names (pair-second entry))))
  (let ((used-names (set-union-many (list-map entry-used-names entries)))
         (fresh-name (generate-relative-fresh-name used-names 'hash))
         (make (apply-exp
                 (qualified-var-exp 'meta-builtin 'builtin 'make-hash location)
                 (@list)
                 location))
         (body (list-fold-right
                 (lambda (entry acc)
                   (begin1-exp
                     (apply-exp
                        (qualified-var-exp 'meta-builtin 'builtin 'hash-put location)
                       (@list (pair-first entry)
                        (pair-second entry)
                        (var-exp fresh-name location))
                       location)
                     acc
                     location))
                 (var-exp fresh-name location)
                 entries)))
    (let1-exp fresh-name make body location)))
