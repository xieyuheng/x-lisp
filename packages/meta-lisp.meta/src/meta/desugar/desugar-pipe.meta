(module meta)

(claim desugar-pipe
  (-> exp-t (list-t exp-t) source-location-t
      exp-t))

(define (desugar-pipe target steps location)
  (if (list-empty? steps)
    target
    (desugar-pipe
      (apply-exp (car steps) [target] location)
      (cdr steps)
      location)))
