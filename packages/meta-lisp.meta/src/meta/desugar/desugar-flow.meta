(module meta)

(claim desugar-flow
  (-> exp-t (list-t exp-t) source-location-t
      exp-t))

(define (desugar-flow target steps location)
  (if (list-is-empty steps)
    target
    (desugar-flow
      (apply-exp (car steps) (@list target) location)
      (cdr steps)
      location)))
