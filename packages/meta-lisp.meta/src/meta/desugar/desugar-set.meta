(module meta)

(claim desugar-set
  (-> (list-t exp-t) source-location-t
      exp-t))

(define (desugar-set elements location)
  (= used-names
     (list-fold-left (lambda (s e) (set-union s (exp-occurred-names e)))
       (make-set) elements))
  (= name (generate-relative-fresh-name 'set used-names))
  (desugar-begin
    (cons
      (assign-exp name
        (apply-exp
          (qualified-var-exp 'meta-builtin 'builtin 'make-set location)
          [] location)
        location)
      (list-append
        (list-map (lambda (e)
                    (apply-exp
                      (qualified-var-exp 'meta-builtin 'builtin 'set-add! location)
                      [e (var-exp name location)]
                      location))
          elements)
        [(var-exp name location)]))
    location))
