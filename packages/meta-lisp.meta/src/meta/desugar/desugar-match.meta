(module meta)

(@comment
  desugar (match), the basic idea is:

  (match target
    ((ctor1 v11 v12 ...) body1)
    ((ctor2 v21 v22 ...) body2)
    ...)

  =>

  (cond
    ((ctor1? target)
     (let* ((v11 (ctor1-accessor1 target))
            (v12 (ctor1-accessor2 target))
            ...)
       body1))
    ((ctor2? target)
     (let* ((v21 (ctor2-accessor1 target))
            (v22 (ctor2-accessor2 target))
            ...)
       body2))
    ...)

  we first need to generalize

  (match target
    (pattern body)
    ...)

  to

  (match-many (target ...)
    ((pattern ...) body)
    ...)

  when all clause heads are var patterns:

  (match-many (target <target> ...)
    ((v1 <pattern> ...) <body>)
    ((v2 <pattern> ...) <body>)
    ...)

  =>

  (match-many (<target> ...)
    ((<pattern> ...) (let ((v1 target)) <body>))
    ((<pattern> ...) (let ((v2 target)) <body>))
    ...)

  when all clause heads are data patterns:

  (match-many (target <target> ...)
    (((ctor1 x1 ...) <pattern> ...) <body>)
    (((ctor1 y1 ...) <pattern> ...) <body>)
    (((ctor2 x2 ...) <pattern> ...) <body>)
    (((ctor2 y2 ...) <pattern> ...) <body>)
    ...)

  first group by head constructors:

  (match-many (target <target> ...)
    (((ctor1 x1 ...) <pattern> ...) <body>)
    (((ctor1 y1 ...) <pattern> ...) <body>)
    ...)

  (match-many (target <target> ...)
    (((ctor2 x2 ...) <pattern> ...) <body>)
    (((ctor2 y2 ...) <pattern> ...) <body>)
    ...)

  then spread the pattern vars in the data patterns:

  (match-many ((ctor1-accessor1 target) ... <target> ...)
    ((x1 ... <pattern> ...) <body>)
    ((y1 ... <pattern> ...) <body>)
    ...)

  (match-many ((ctor2-accessor1 target) ... <target> ...)
    ((x2 ... <pattern> ...) <body>)
    ((y2 ... <pattern> ...) <body>)
    ...)

  to handled mixed head patterns,
  we first group clauses by head pattern kind.)

(define-struct desugar-match-ctx-t
  (scope fragment-scope-t)
  (current-mod-name symbol-t)
  (algebraic-analysis-report algebraic-analysis-report-t)
  (pkg-name symbol-t))

(claim desugar-match
  (-> desugar-match-ctx-t (list-t exp-t) (list-t match-clause-t) exp-t source-location-t
      exp-t))

(define (desugar-match ctx targets clauses default-exp location)
  (for-list clauses
    (lambda (clause)
      (unless (equal? (list-length (match-clause-patterns clause)) (list-length targets))
        (error-with-location
          (@string "(desugar-match) targets length mismatch"
                   "\n  targets: " (format-exps targets)
                   "\n  patterns: " (format-exps (match-clause-patterns clause)))
          location))))
  (cond
    ((list-empty? clauses) default-exp)
    ((list-empty? targets) (match-clause-body (car clauses)))
    ((list-every? var-pattern-clause? clauses)
     (desugar-match
       ctx (cdr targets)
       (list-map (desugar-var-pattern-clause (car targets)) clauses)
       default-exp location))
    ((list-every? data-pattern-clause? clauses)
     (cond-exp
       (list-map
         (lambda (group)
           (desugar-data-pattern-clause-group
             ctx group targets default-exp location))
         (group-clauses-by-head-data-constructor ctx clauses))
       location))
    (else
     (list-fold-right
       (lambda (group folded)
         (desugar-match ctx targets group folded location))
       default-exp
       (group-clauses-by-head-pattern-kind clauses)))))

(define (desugar-var-pattern-clause target clause)
  (@comment
    target ((v1 <pattern> ...) <body>)
    =>
    ((<pattern> ...) (let ((v1 target)) <body>)))
  (match clause
    ((make-match-clause patterns body location)
     (= pattern (car patterns))
     (unless (var-exp? pattern)
       (error-with-location
         "(desugar-var-pattern-clause) expect var-pattern"
         (exp-location pattern)))
     (make-match-clause
       (cdr patterns)
       (let1-exp (var-exp-name pattern) target body location)
       location))))

(define (var-pattern-clause? clause)
  (and (not (list-empty? (match-clause-patterns clause)))
       (var-pattern? (car (match-clause-patterns clause)))))

(define (data-pattern-clause? clause)
  (and (not (list-empty? (match-clause-patterns clause)))
       (data-pattern? (car (match-clause-patterns clause)))))

(define-struct data-pattern-clause-group-t
  (info data-constructor-info-t)
  (clauses (list-t match-clause-t)))

(define (desugar-data-pattern-clause-group ctx group targets default-exp location)
  (@comment
    (match-many (target <target> ...)
      (((ctor1 x1 ...) <pattern> ...) <body>)
      (((ctor1 y1 ...) <pattern> ...) <body>)
      ...)
    then spread the pattern vars in the data patterns:
    (match-many ((ctor1-accessor1 target) ... <target> ...)
      ((x1 ... <pattern> ...) <body>)
      ((y1 ... <pattern> ...) <body>)
      ...))
  (match group
    ((make-data-pattern-clause-group
       (make-data-constructor-info
         pkg-name mod-name type-name name
         accessor-names predicate-name)
       clauses)
      (define (spread-target target)
        (list-map
          (lambda (accessor-name)
            (apply-exp
              (qualified-var-exp pkg-name mod-name accessor-name location)
              [target]
              (exp-location target)))
          accessor-names))
      (make-cond-clause
        (apply-exp
          (qualified-var-exp pkg-name mod-name predicate-name location)
          [(car targets)]
          (exp-location (car targets)))
       (desugar-match
         ctx
         (list-append (spread-target (car targets)) (cdr targets))
         (list-map clause-spread-first-data-pattern clauses)
         default-exp location)
       location))))

(define (clause-spread-first-data-pattern clause)
  (match clause
    ((make-match-clause patterns body location)
     (define (spread-data-pattern pattern)
       (unless (data-pattern? pattern)
         (error-with-location
           (@string "(spread-data-pattern) expect data pattern"
                    "\n  pattern: " (format-exp pattern))
           (exp-location pattern)))
       (apply-exp-args pattern))
     (make-match-clause
       (list-append (spread-data-pattern (car patterns)) (cdr patterns))
       body
       location))))

(define (group-clauses-by-head-data-constructor ctx clauses)
  (= algebraic-type-info (lookup-same-algebraic-type ctx clauses))
  (match (lookup-same-algebraic-type ctx clauses)
    ((make-algebraic-type-info pkg-name mod-name name constructor-names)
     (swap list-map constructor-names
       (lambda (ctor-name)
          (= key (algebraic-key pkg-name mod-name ctor-name))
         (= data-constructor-info
            (pipe ctx
              desugar-match-ctx-algebraic-analysis-report
              algebraic-analysis-report-data-constructor-infos
              (hash-get key)))
         (make-data-pattern-clause-group
           data-constructor-info
           (list-select
             (clause-starts-with-data-constructor? ctx data-constructor-info)
             clauses)))))))

(define (clause-starts-with-data-constructor? ctx info clause)
  (= pattern (car (match-clause-patterns clause)))
  (if (not (data-pattern? pattern))
    false
    (match (resolve-data-pattern-qualified-name ctx pattern)
      ((make-qualified-name pkg-name mod-name name)
       (and (equal? (data-constructor-info-pkg-name info) pkg-name)
            (equal? (data-constructor-info-mod-name info) mod-name)
            (equal? (data-constructor-info-name info) name))))))

(define (lookup-algebraic-type-info ctx clause)
  (= pattern (list-first (match-clause-patterns clause)))
  (unless (data-pattern? pattern)
    (error-with-location
      (@string "[lookup-algebraic-type-info] expected data pattern"
        "\n  pattern: " (format-exp pattern))
      (match-clause-location clause)))
  (= qn (resolve-data-pattern-qualified-name ctx pattern))
  (= pkg-name (qualified-name-pkg-name qn))
  (= mod-name (qualified-name-mod-name qn))
  (= name (qualified-name-name qn))
  (= info (hash-get-maybe (algebraic-key pkg-name mod-name name)
           (algebraic-analysis-report-data-constructor-infos
             (desugar-match-ctx-algebraic-analysis-report ctx))))
  (match info
    ((nothing)
     (error-with-location
       (@string "[lookup-algebraic-type-info] undefined data constructor"
         "\n  pkg-name: " (symbol-to-string pkg-name)
         "\n  mod-name: " (symbol-to-string mod-name)
         "\n  name: " (symbol-to-string name)
         "\n  key: " (symbol-to-string pkg-name) "/" (symbol-to-string mod-name) "/" (symbol-to-string name))
       (match-clause-location clause)))
    ((just i)
     (= algebraic-type-info (hash-get-maybe
                              (algebraic-key (data-constructor-info-pkg-name i)
                                (data-constructor-info-mod-name i)
                                (data-constructor-info-type-name i))
                              (algebraic-analysis-report-algebraic-type-infos
                                (desugar-match-ctx-algebraic-analysis-report ctx))))
     (match algebraic-type-info
       ((nothing)
        (error-with-location
          (@string "[lookup-algebraic-type-info] cannot find algebraic type info"
            "\n  constructor name: " (symbol-to-string (data-constructor-info-name i))
            "\n  type name: " (symbol-to-string (data-constructor-info-type-name i))
            "\n  module name: " (symbol-to-string (data-constructor-info-mod-name i)))
          (match-clause-location clause)))
       ((just ti) ti)))))

(define (lookup-same-algebraic-type ctx clauses)
  (= first (lookup-algebraic-type-info ctx (list-first clauses)))
  (for-list (list-tail clauses)
    (lambda (clause)
      (= current (lookup-algebraic-type-info ctx clause))
      (unless (and (equal? (algebraic-type-info-pkg-name current) (algebraic-type-info-pkg-name first))
                   (equal? (algebraic-type-info-mod-name current) (algebraic-type-info-mod-name first))
                   (equal? (algebraic-type-info-name current) (algebraic-type-info-name first)))
        (error-with-location
          (@string "[lookup-same-algebraic-type] algebraic data type mismatch"
            "\n  first: " (symbol-to-string (algebraic-type-info-pkg-name first))
            "/" (symbol-to-string (algebraic-type-info-mod-name first))
            "/" (symbol-to-string (algebraic-type-info-name first))
            "\n  current: " (symbol-to-string (algebraic-type-info-pkg-name current))
            "/" (symbol-to-string (algebraic-type-info-mod-name current))
            "/" (symbol-to-string (algebraic-type-info-name current)))
          (match-clause-location clause)))))
  first)

(define (resolve-data-pattern-qualified-name ctx pattern)
  (unless (data-pattern? pattern)
    (error-with-location
      "(resolve-data-pattern-qualified-name) expect data-pattern"
      (exp-location pattern)))
  (= target (apply-exp-target pattern))
  (match target
    ((qualified-var-exp pkg-name mod-name name location)
     (make-qualified-name pkg-name mod-name name))
    ((var-exp name location)
     (= scope (desugar-match-ctx-scope ctx))
     (= imported-names (fragment-scope-imported-names scope))
     (match (hash-get-maybe name imported-names)
       ((just e)
        (make-qualified-name
          (qualified-name-pkg-name e)
          (qualified-name-mod-name e)
          (qualified-name-name e)))
       ((nothing)
        (make-qualified-name
          (desugar-match-ctx-pkg-name ctx)
          (desugar-match-ctx-current-mod-name ctx)
          name))))
    (else
     (error-with-location
       "(resolve-data-pattern-qualified-name) unhandled target"
       (exp-location target)))))

(claim group-clauses-by-head-pattern-kind
  (-> (list-t match-clause-t)
      (list-t (list-t match-clause-t))))

(define (group-clauses-by-head-pattern-kind clauses)
  (= groups [])
  (for-list clauses
    (lambda (clause)
      (if (list-empty? groups)
        (ignore (list-push! [clause] groups))
        (let ((last (list-last groups)))
          (if (same-pattern-kind? clause (list-first last))
            (ignore (list-push! clause last))
            (ignore (list-push! [clause] groups)))))))
  groups)

(define (same-pattern-kind? a b)
  (equal? (var-pattern-clause? a) (var-pattern-clause? b)))

(define (make-default-exp targets location)
  (= parts [(string-exp "pattern mismatch, no match-clause for targets:" location)])
  (for-list targets
    (lambda (target)
      (list-push! (string-exp " " location) parts)
      (list-push!
        (apply-exp
          (qualified-var-exp 'meta-builtin 'builtin 'format location)
          [target]
          location)
        parts)))
  (apply-exp
    (qualified-var-exp 'meta-builtin 'builtin 'error location)
    [(string-concat-exp parts location)]
    location))
