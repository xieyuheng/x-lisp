(module compiler)

(define (check-pipeline root-package)
  (let ((closure (meta/package-closure-in-topological-order root-package)))
    (list-each closure meta/expand-pass)
    (list-each closure meta/module-prelude-pass)
    (let ((module-reports (make-hash)))
      (list-each closure
        (lambda (package)
          (hash-put module-reports (meta/package-id package)
            (meta/module-analysis-pass package))))
      (let ((algebraic-reports (make-hash)))
        (list-each closure
          (lambda (package)
            (hash-put algebraic-reports (meta/package-id package)
              (meta/algebraic-analysis-pass package))))
        (list-each closure
          (lambda (package)
            (meta/lower-match-pass
              (hash-get module-reports (meta/package-id package))
              (hash-get algebraic-reports (meta/package-id package))
              package)))
        (list-each closure meta/desugar-pass)
        (list-each closure
          (lambda (package)
            (meta/module-import-pass
              (hash-get module-reports (meta/package-id package)) package)))
        (list-each closure meta/setup-pass)
        (list-each closure meta/claim-pass)
        (list-each closure meta/qualify-pass)
        void))))
