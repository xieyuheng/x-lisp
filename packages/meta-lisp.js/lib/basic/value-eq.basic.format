(claim check-eq (-> value-t value-t bool-t))

(define-function (check-eq x y)
  (block body
    (= x (argument :index 0))
    (= y (argument :index 1))
    (= same (value-eq x y))
    (return same)))
