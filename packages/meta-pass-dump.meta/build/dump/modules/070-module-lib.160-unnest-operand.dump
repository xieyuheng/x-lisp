(claim greeting string-t)

(define greeting "Hello")

(claim greet (-> string-t string-t))

(define (greet name)
  (= unnested.1
     (meta-builtin/builtin/string-append self/070-module-lib/greeting ", "))
  (meta-builtin/builtin/string-append unnested.1 name))