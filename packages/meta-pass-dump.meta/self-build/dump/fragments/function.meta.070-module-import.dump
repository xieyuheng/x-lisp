(import-all meta-builtin/builtin)

(module function)

(define (identity x) x)

(define (compose2 f g x) (f (g x)))

(define (double x) (meta-builtin/builtin/iadd x x))

(define answer (the meta-builtin/builtin/int-t 42))