(module identity-type-error-2)

(claim identity
  (all (A)
    (-> A A)))

(define (identity x) x)

(define-test identity-test
  (the (all (A) (-> A A)) identity)
  (the (all (A B) (-> A B)) identity))
