---
title: tuple
author: xieyuheng
date: 2026-08-04
---

如果想保持简单类型系统，
就没法实现任意可变长度的 tuple，
因为 tuple-get 和 tuple-put 的参数类型是结构化多态的，
如果引入了这种多态，就引入了子类型关系。

代替方案是逐个实现二元组、三元组、四元组、等等。
可能需要内置于语言，来避免 `(define-struct)` 的 variant 的 symbol tag。
