;;; Input

(define (_∑-top-level-computation)
  (println-non-void (begin (= x 4) (iadd 8 x))))

;;; ShrinkPass

(define (_∑-top-level-computation)
  (println-non-void (@let1 x 4 (iadd 8 x))))

;;; UniquifyPass

(define (_∑-top-level-computation)
  (println-non-void (@let1 x₁ 4 (iadd 8 x₁))))

;;; RevealFunctionPass

(define (_∑-top-level-computation)
  ((@function println-non-void 1)
   (@let1 x₁ 4 ((@function iadd 2) 8 x₁))))

;;; LiftLambdaPass

(define (_∑-top-level-computation)
  ((@function println-non-void 1)
   (@let1 x₁ 4 ((@function iadd 2) 8 x₁))))

;;; UnnestOperandPass

(define (_∑-top-level-computation)
  (@let1 _₁ (@function println-non-void 1)
    (@let1 x₁ 4
      (@let1 _₂ (@function iadd 2)
        (@let1 _₃ 8 (@let1 _₄ (_₂ _₃ x₁) (_₁ _₄)))))))

;;; ExplicateControlPass

(define-function _∑-top-level-computation
  (block entry
    (= _₁ (const (@function println-non-void 1)))
    (= x₁ (const 4))
    (= _₂ (const (@function iadd 2)))
    (= _₃ (const 8))
    (= _₄ (apply _₂ _₃ x₁))
    (= _↩ (apply _₁ _₄))
    (return _↩)))

