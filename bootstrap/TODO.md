[bootstrap] [lang] import prefix should be explicit -- no implicit `/`
[bootstrap] [basic] import prefix should be explicit -- no implicit `/`

[bootstrap] in-place build commands
[design] more example about cicada-lisp
